# Startup readiness and performance

Status: Deferred ordinary collection counts owner-confirmed; whole-branch review remediation verified
Baseline: source checkpoint `0bada870`; integrating main `e50926487c68e433b85009628705832a9d6be039` (v0.13.2, verified 2026-09-07)

## Current package: open the safe gallery before ordinary counts finish

Approved 2026-09-07. Separate scoped collection metadata readiness from ordinary membership-count completion. During initial startup, publish collection rows with explicit pending counts without running the aggregate. After the existing safe-gallery readiness signal, start one deferred read without waiting for source catch-up. Ownership/privacy admission, smart-count/thumbnail cache preparation, gallery/global totals, maintenance, facets and native scope-cache transactions remain unchanged. This explicitly replaces the earlier requirement that ordinary collection badges be fully counted before opening; it does not relax the safe-page requirement.

The count SQL is extracted unchanged. A store-owned single-flight path coordinates background and authoritative reads using initialization generation and refresh revision; superseded results cannot overwrite current scope or metadata. Requests coalesce, paused results remain pending, errors do not automatically retry, and an accessible Retry counts action requests a new read. The first-readiness flag is sticky: later runtime owner/privacy transitions retain awaited authoritative refreshes. No SQL cancellation, pool closure or new shutdown wait is introduced.

Ordinary count state is pending/ready/failed. Pending and failed counts never fall back to an empty membership array or an old numeric value. Lists/pickers show a dash with Updating count or Count unavailable; unknown counts sort last. An unknown active-collection denominator remains pending while verified filtered results remain displayed. Unknown ordinary collections remain eligible for bounded thumbnail hydration. Count-only updates preserve image-query identity and selection. Browser/legacy complete-membership behavior and smart counts retain their existing semantics.

Verification: the deterministic held-count initialization regression failed against the old implementation; the metadata-first path passes it. Focused repository/store/App/UI tests cover metadata-only SQL, generation rejection, single-flight/coalescing, runtime awaited success/failure after readiness withdrawal, pending/error/retry, unknown thumbnail eligibility, stable gallery query keys and post-ready count diagnostics. Final frontend regressions pass 3,553 tests (one intentional skip); native regressions pass 986 library and 18 utility tests (28 ignored). Typecheck, lint, version/binding checks, whitespace checks and the guarded frontend build pass. The serial release-profile desktop compatibility build also passes; no application was launched. The extracted ordinary SQL matches the original exactly after newline normalization; shipping migrations, dependencies and bindings are untouched. Existing journal milestones and bounds remain unchanged; ordinary counts completing afterward are not ranked as startup-blocking work.

The initial concurrency/ownership and specification/UI closure reviews were clean. Review found that newly discovered InvokeAI boards still supplied an unverified zero; those rows now start pending, with a failing-before/fixed-after Sync integration assertion and a store regression for an empty opened list receiving its first board. Concerns about pre-first-readiness metadata refresh and retry on a legitimate subsequent refresh were withdrawn after checking the explicit phase requirements. The subsequent whole-branch review found two further issues, recorded below rather than treating the earlier closure as merge approval.

The approved three-launch regular-dev/All-users batch is complete. First-after-change launch `2a2d7f7d-152f-4b06-af0d-d1c17d1b2ce8` recorded readiness 15.052s, ordinary counts starting 15.054s and completing 17.546s. Unchanged launch `b1c54cb0-ef87-4f0a-9420-1da28a29a34d` recorded readiness 6.298s, counts starting 6.300s and completing 8.501s. Eight-image launch `4cfb950d-0951-4322-954a-5464b3883c4b` selected incremental catch-up, recorded splash dismissal 8.547s, readiness 15.110s, initial counts starting 15.116s/completing 18.057s, and catch-up completion 30.755s. A legitimate later count refresh completed at 27.088s. The owner confirmed responsiveness, pending dashes becoming numbers, correct-looking counts and image arrival. The privacy-protected gallery took a few seconds to open, then was immediately responsive. No further startup batch is requested. These native-receipt timings are not OS-cold measurements, pixel-paint proof, or general performance/reliability acceptance.

No persistent count cache, schema/migration, query/index/pool-policy change, catch-up optimization, automatic app launch, library copy/access, production or credential changes. Both failed index gates remain below. The previous comparison baseline was warm median 9.612s, changed-source readiness 35.808s and catch-up 104.861s. The latest warm/changed runs meet the under-10s/under-30s targets individually, not as general acceptance. Persistent ordinary-count reuse with transactional invalidation remains optional separate work, not required to finish this improvement or inferred from the smart/thumbnail owner-cache ready flag. The historical intermittent black window remains unresolved.

### Whole-branch review remediation

Review pinned HEAD `e15e806e` against main/merge-base `e5092648`, including all dirty/new source files; remote main matched. The 157-file review snapshot and unchanged hashes are retained in `src-tauri/target/whole-review-inventory.json`, with findings/verification in `whole-review-report.md`. No shipping index was integrated.

- **SPEC-1 (P2):** a failed refresh superseding pending startup counts could leave badges permanently pending without retry. The current generation/revision now marks only pending ordinary counts failed, retaining authoritative rejection and stale-owner safety. Combined failure/retry and obsolete-owner regressions were added; the former failed before the fix.
- **STD-1 (P2):** ordinary renderer diagnostic commands synchronously wrote to the journal on native UI dispatch. They now enqueue validated, timestamped events to a bounded 320-entry queue drained by the existing observer. Storage/logging occur outside the queue lock; validated ordinary/terminal/repair allowances fit without consuming one another's capacity. Teardown does not wait for the writer. A held-storage regression failed before the fix and passes afterward.
- **STD-1 closure:** review identified that queued frontend entry must count as received before persistence, otherwise the missing-renderer observation can be false. An admission-time atomic flag now preserves that distinction; the deterministic regression failed before the correction and passes afterward.
- **DOC-1:** corrected the completed owner batch above and removed stale requests for more launches.

Remediation verification passed: 3,555 frontend tests (one intentional skip), 991 default native library tests plus 18 utility tests (28 ignored), and 1,037 diagnostic-feature library tests plus 18 utility tests (36 ignored). Focused count/journal regressions, controller self-tests, typecheck, lint, version/binding checks, scoped Rust formatting, whitespace checks, guarded frontend build, serial release-profile no-bundle desktop build and diagnostic-mode compilation passed. Independent Standards and Specification closure reviews are clean. Snapshot comparison confirms only the seven intended remediation code/test/documentation files changed; dependencies, bindings, queries and migrations remain unchanged. Ready for merge consideration, not automatically committed or merged. No new owner tests, app launches, business command/binding changes, schema changes or performance-policy changes are part of this remediation. Historical blank-window attribution and general startup acceptance remain unresolved.

## Completed package: qualify exactly one post-m80 collection index

The completed owner batch confirmed image arrival and gallery responsiveness. Warm readiness was 9.345s and 9.878s (median 9.612s); eight-image readiness was 35.808s, including 22.512s collection counting. Maintenance stayed at 61–75ms. Admission was incremental catch-up, which completed at 104.861s. The first-after-upgrade launch reached readiness at 32.371s and included 19.556s schema initialization; that is not an isolated migration-SQL measurement. Journals: `fc199e95-d376-4c67-ba40-4c3d94aa0e2f`, `49a355ab-07ca-4001-8829-f98baa166051`, `9426f9bf-b0b9-4497-8808-af3ff53420e8`, `7714a034-e057-4e6d-a5b0-2b2344bbedfe`. The previous owner instructions below are completed, not another requested batch.

Approved candidate: `idx_images_collection_count_inputs` on `images(id, invoke_source_id, invoke_owner_id, invoke_scope_hidden)`, with index-specific analysis. Keep shipping SQL/views, migration 80, settings and scheduling unchanged. The earlier small-library maintenance failure remains binding historical evidence; m80 permits this separately labelled qualification, not reuse of prior acceptance.

Gates: small generated access/ownership/gallery correctness first; then four ordered IPC cells (10k All users, 10k selected owner, 10k genuinely local, 146,182/131,741 All users), six counterbalanced fresh-process pairs each, initial plus three warm concurrent rounds. Preparation/copies stay in the controller-owned preparer. Apply identical generated-only uniform defaults to ten verified physical connections. Require exact results, large warm collection gain of at least 20% and 200ms in at least five paired directions, and 10%/+20ms non-regression for other count comparisons and complete native spans. A single 30-minute inclusive campaign and 60-second external round limit stop on the first failed cell; no rerun or substitute.

Large-fixture gates also cover creation time, added allocation below 64MiB, six paired rollback insert/update/delete samples, and fresh-worker peak working set/private commit within baseline +128MiB. Missing measurements are incomplete. Only all gates passing permits migration 81 after rechecking upstream availability, followed by migration/regression/build verification and the separately specified owner check. No app launches, real-library access or retained-artifact cleanup.

The access gate passed on bundled SQLite 3.46.0: candidate collection plans have zero image-table Column operations before/after refreshed statistics; collection and maintenance results match; normal gallery sorting stays indexed. Fixed generated ownership/membership transitions pass. Initial compilation stopped for full C: storage; the owner approved deletion of only regenerable `target/debug/incremental`, leaving 32.78GiB free before rebuilding. No library or historical evidence was removed.

Single campaign `3619b1ea-7185-4f62-8360-8e29eca47e36` completed all 48 fresh workers, 192 rounds and 768 matched IPC/native calls in 689.17s. Exact results, ten-connection settings/lifetimes, trace completeness and all small-library gates passed. Its terminal `overhead-gate-failed` is the retained controller-compatible qualification-failure label, not a finding about tracing overhead. No timeout, rerun, forced termination or migration 81 occurred.

Large All-users warm collection retrieval improved from 2,972.3570 to 1,105.6839ms (62.80%, faster in all six pairs); warm four-query native span fell from 2,973.3344 to 1,475.5566ms. However, first-read gallery retrieval rose from 9,373.3009 to 11,852.2769ms (+26.45%), its matched IPC comparison also failed, and first-read span rose from 9,603.8713 to 11,852.4456ms (+23.41%). These three comparisons exceed the agreed allowance. Candidate first gallery reads were faster in four of six individual pairs: high variability does not establish a consistent causal regression, but does not override the predeclared median gate. Generated first reads are not OS-cold startup measurements.

Other large warm retrieval medians (baseline/candidate ms): maintenance 49.6057/48.9063, gallery 1,646.5542/1,475.4437, global 40.1050/39.0694. Index creation took 12,213.3589ms and added 14,999,552 allocated bytes (14.30MiB). Six-pair rollback transaction medians passed: inserts 83.4002/93.2779ms, owner updates 29.6372/30.6716ms, deletes 35.9537/34.7638ms; the sub-200ms baselines use the +20ms allowance. Fresh-worker peak working-set medians were 62,310,400/62,355,456 bytes and peak private commit 45,971,456/45,936,640 bytes, both within +128MiB. These are whole generated-worker measures, not application memory estimates.

Evidence: `src-tauri/target/startup-sql-post-m80-index-3619b1ea-7185-4f62-8360-8e29eca47e36.jsonl`, controller companion and 48 arm reports retain individual readings, medians, slowest reads, settings and provenance. Root report SHA-256 `de1a393b73c31b50a63c08044ebb77973a67737c1d16bb6ccda75c0c3177524b`; controller report `8f4be7d534d88b99239c484fcd1ecda34fc6c81665288e066eb1682c8e5e8a65`; executable `710213e4c298887bcb7f23e974b9016df30268543e8ecce67cbf7d27294175ee`, revision `e15e806e6ad7e71b37eb05e9a110c818124ffb95`. Independent evidence review recomputed every gate: 62/64 count comparisons and 7/8 span comparisons pass; both memory and all three write gates pass. The 344,452-byte report contains 2,351 ordered records, zero drops and no unavailable storage.

Verification passed: six focused regressions (three opt-in tests ignored), tiny two-worker protocol smoke, controller self-tests/syntax, full frontend 3,531 passed/one skipped, default native 986 library +18 utility tests, diagnostic native 1,032 library +18 utility tests, typecheck, lint, versions, binding drift, scoped Rust formatting/whitespace, guarded frontend build and serial normal/diagnostic no-bundle desktop builds. Independent safety and evidence closure reviews are clean. Existing build advisories remain. Shipping source, queries, views, dependencies, bindings and migrations are unchanged; conditional migration-81 installation/upgrade tests and its owner batch are correctly unrun after the failed gate.

Cleanup recorded 56 guarded removals (eight templates and 48 arms); metadata-only postflight confirms the new `D:/Ambit-Post-M80-Index-ec608650-c9d7-4182-a0b9-0681c9d4385a` root is empty. No older artifacts were accessed or removed. Decision: stop integration, preserve both this and the earlier failed index gate. Recommend a separately approved, narrow investigation of variable first-read gallery access/cache behavior before any requalification; do not infer serialization or a shipping optimization from overlap alone. No owner batch is requested. Real startup targets, catch-up performance and the historical blank-window cause remain unresolved.

Normal cleanup is guarded and recorded, including early unwind. At forced termination during preparation, the controller may kill its preparer to honor the hard budget; cleanup is explicitly unobserved and retained generated directories must not be manually deleted. During an active measured child, stop that child first and allow bounded preparer teardown before forced fallback. This limitation is not a successful cleanup claim.

## Completed package: integrate upstream maintenance-count fix

- User-approved local checkpoint preserves the accumulated startup work; nothing is pushed. The integration includes PR #313 and migration 80 plus the preceding Removed-listing fix. Upstream SQL, index definitions and ownership semantics are retained; the maintenance query uses the existing optional tracing wrapper.
- Historical measurements below are **pre-migration-80 / old maintenance query**. The failed index/policy gates and incomplete ownership campaigns remain historical, not automatically requalified. Old campaign entrypoints now reject migration 80 before preparation; the controller rejects before report creation or worker launch. Historical policy validators retain their original query digest rather than adopting current SQL.
- No new performance policy, library copy/access, app launch, production operation or retained-artifact cleanup is part of implementation. Normal `pnpm run app:dev` remains tracing-disabled. Migration 80 will first affect regular dev when the owner explicitly launches it after verification.
- Verification: targeted frontend integration 44 passed; full frontend 3,531 passed, one skipped. Normal native 984 library + 18 utility tests passed (28 ignored); diagnostic-feature native 1,026 library + 18 utility tests passed (33 ignored). Actual MockRuntime plugin coverage verifies migration 79-to-80 upgrade, prior checksums, reload and maintenance tracing enabled/disabled/unavailable. Typecheck, lint, versions, binding drift, scoped formatting/whitespace, controller self-tests, guarded frontend and serial default release-profile no-bundle desktop build passed. Existing native dead-code and frontend build advisories remain. No installer publication or owner acceptance was run.
- Initial native verification exposed an existing uptime-dependent test: subtracting an hour from Windows' monotonic clock could underflow. The generated preparation helper now accepts an explicit limit; only the expiry test uses zero, all campaign/other callers retain the approved limit. The final suites pass. Independent migration/lifecycle and specification closure reviews are clean; historical report hashes are unchanged.
- Owner check after verification: All users, regular dev, one first-after-upgrade launch, two unchanged launches, then one launch after eight new Odin images. Read each automatic journal before continuing. The first launch includes one-time index creation; keep it separate. Owner reports responsiveness, maintenance badges and image arrival; no stopwatch. If blank, leave open about 60 seconds and stop. Stop on migration/visibility/count errors; no automatic repair or downgrade.
- The post-integration batch is recorded above. Targets remain under 10s warm and under 30s changed-source readiness; this small batch does not establish general performance or reliability acceptance.

## Outcome and invariants

Keep the fully prepared opening experience and all owner/privacy gates. Routine startup must not compete with optional prompt analysis or timer-triggered maintenance. Reuse unaffected scope caches after thumbnail repairs. The historical 7.5-minute startup is not fully attributed; do not claim it fixed without a measured native journey.

User stories:
1. As a library user, I want startup to prioritize preparing my library, so background maintenance does not delay opening.
2. As a Statistics user, I want the word cloud calculated when I open Statistics, so ordinary browsing avoids prompt scans.
3. As an InvokeAI user, I want unaffected cached data reused, so thumbnail repairs do not unnecessarily slow subsequent launches.
4. As a privacy-conscious user, I want owner/privacy checks preserved during failures and transitions, so faster startup never exposes inappropriate content.
5. As a maintainer, I want attributable local startup timings, so regressions can be diagnosed.

## Work packages

### Latest stopped package: ownership comparison within 60 minutes

Owner approved the full plan on 2026-09-06. Change only the ownership campaign's Rust and controller budget to 3,600,000ms and record the effective worker budget. Preserve the 60-second round deadline, tracing/lifecycle behavior, six validated templates, 72 fresh arms, 288 rounds, 1,152 calls, queries/views, uniform-default treatment and six-block material-penalty gate. Verify deadline boundaries, preparation expiry, template isolation and evidence handling before one new generated-only run. Compilation/regressions are outside the campaign budget; fixture preparation and measurements are inside it.

Use a fresh ordinary D: generated root with at least 32 GiB free, process-local TEMP/TMP only, immutable preflight/executable linkage and create-new bounded reports. Do not inspect, reuse or clean older generated databases. Stop on the first invalid comparison, failure or deadline; no extension or automatic rerun. After verification and independent deadline/safety review, run once, independently review the final evidence, and deliver a reproduced ownership-view finding or explicit incomplete result. No app launches, real-library access, owner testing, shipping changes or target-acceptance claims.

Implementation and verification completed: Rust and controller use 3,600,000ms, and both report the effective budget. Duration/injected-clock tests cover just-before/at expiry and the unchanged 60-second round boundary; the controller's extracted stop seam preserves owned-child-only and idempotent termination. The deterministic native regression failed against 2,700,000ms before passing at 3,600,000ms (`ownership-hour-red.log` / `ownership-hour-green.log`). Full feature-enabled native regressions pass 1,018 library + 18 utility tests, with 32 opt-in tests ignored (`ownership-hour-native.log`, 299.17s library suite). Frontend 3,502 tests with one skip, typecheck, lint, controller self-tests/syntax, scoped Rust formatting and whitespace checks pass. Independent standards/deadline-safety and spec/evidence reviews are clean. No default-feature full native suite, binding regeneration or production frontend/desktop build was performed for this test-only budget change.

Single campaign `fac6c708-4064-4693-bc34-7a22751ede7e` ended at the 60-minute controller deadline: 3,600,238.3716ms, `timed-out` / child `incomplete`, 244/288 rounds, no pending round, only the owned generated worker terminated. All five initial blocks completed; block six includes only selected-owner-bypass concurrent reads. Its sequential arm was at `fixture-copy` before reads when interrupted. This is a last-known stage, not a reported copy/setup error. No query/setup failure was recorded, but missing 44 rounds/176 calls precludes the required six-block comparisons.

Independent final evidence closure confirms 2,954 contiguous records / 363,456 bytes, six validated templates, 31 linked admissions, 976 unique exact-result IPC/native pairs, 244 overlap/group-span records, 122 fixed ten-connection snapshots and 30 equal boundary comparisons. No worker terminal exists after forced termination; the controller retains the timeout. Template preparation completed before the first arm at 887.013s (14.78 minutes). The larger budget did not establish campaign feasibility; do not pool partial campaigns, calculate a six-block material penalty from five blocks, or attribute historical startup time from these samples.

Preflight used a fresh ordinary `D:/Ambit-Ownership-Hour-b141e416-1d94-48c5-8e3d-1d8fb8d678e4` root and 1,386,240,540,672 available bytes, above the 32 GiB gate. TEMP/TMP changed only in transient controller/child processes. Sixty-one generated arms recorded `removed`; metadata-only postflight observed eight ordinary generated directories remaining (base, six templates and interrupted arm), with cleanup unobserved after termination. No manual cleanup, reuse or older-directory access occurred. Retain this root for a separately approved cleanup, not reuse.

Create-new artifacts under `src-tauri/target`: `startup-sql-ownership-fac6c708-4064-4693-bc34-7a22751ede7e.jsonl` and controller companion, `ownership-hour-preflight-b141e416-1d94-48c5-8e3d-1d8fb8d678e4.json`, and matching campaign link/postflight records. Worker SHA-256 `fd0f99397412161a00341ee7747f6253c1e8832dc987649dca093b1693a7dc24`; controller `9b6985b8cf402a4e084a09f5372b41ed82f20f264e02546ab4040a3b72807d05`. Executable SHA-256 `765711b3ec400ca88c7144b9c7f7a2613bf420b3380e50b38e845c24a312226c`. Protected source aggregate stayed `12029267ED4CF151564F2E8D3E1483D3456FF9B3DD31507B28E2367806C7D654` during execution; the pre-edit reconstruction matches the prior aggregate, proving only the two approved test sources changed. All three older ownership report pairs retain their original hashes.

Decision: inconclusive ownership attribution. Stop this campaign approach at its agreed gate rather than automatically raise the timer again. A separate plan should make the experiment reliably completable in bounded units while preserving predeclared comparisons and avoiding cherry-picking; no such redesign or performance change is implemented here. Historical setup failure, black-window cause, catch-up and real startup acceptance remain unresolved. Latest owner readiness/catch-up remains 37.422s/57.076s; no owner batch requested and normal development remains tracing-disabled.

### Latest stopped package: one revised-template ownership campaign

Owner approved on 2026-09-06 following template-reuse verification. Run the existing six-condition campaign once, with six prepared templates, 72 fresh boundary copies/registrations, 288 rounds and 1,152 calls. Preserve all fixed queries, uniform-default connection treatment, counterbalancing, equality/settings gates, 45-minute preparation-plus-measurement budget and 60-second round deadline. No timing-driven rerun, real-library access, app launch, shipping change or owner batch.

Preflight: controller self-tests/syntax and Cargo no-run pass against the verified feature executable. D: has 1,407,876,325,376 free bytes, exceeding the 32 GiB gate. A fresh ordinary generated root uses identifier `62fea8cf-8e31-4fd6-8dd0-745d1588c6f7`; TEMP/TMP will change only in the controller process and its children. Create-new `ownership-template-preflight-62fea8cf-8e31-4fd6-8dd0-745d1588c6f7.json` records the source/executable hashes and fixed policy. Prior timed-out directories and reports remain untouched. Deliver independently checked complete comparisons or a truthful partial/inconclusive result; do not combine different preparation policies or infer real startup attribution.

Outcome: campaign `7821d90c-dabb-4b3b-a3dd-9e9af6b8ee5b` stopped once at 2,700,407.1847ms: controller `timed-out`, child `incomplete`, 248/288 completed rounds and no pending round. Five full blocks and selected-owner bypass in block six completed (992/1,152 calls). The next local-shipping arm was at `fixture-copy` before any reads; this is a last-known stage, not a reported copy error. No query/setup failure was recorded. The required six-block comparisons remain unrun, and no material ownership penalty or historical startup attribution is supported.

Independent final evidence review is clean for this partial classification: 369,882 bytes, 3,002 contiguous records, six ordered validated templates, 32 valid admission links, 992 unique exact-result IPC/native pairs, 248 group spans, 124 fixed ten-connection pool snapshots and 31 equal boundary comparisons. The worker has no terminal after controller termination; missing cleanup remains unknown. All six templates were available when the first arm began at 882.036s (14.70 minutes). Completed copy-stage-to-next-stage spans total 838.028s across 68 copies; these are observed wall intervals, not pure SQLite execution, and exclude the interrupted final copy. Preparation reuse worked, but the full matrix still did not fit the 45-minute budget.

Cleanup: 62 generated arms recorded `removed`. Metadata-only postflight observed eight ordinary generated directories remaining in `D:/Ambit-Ownership-Templates-62fea8cf-8e31-4fd6-8dd0-745d1588c6f7` (base, six retained template scopes and interrupted arm have unobserved cleanup). No manual deletion, retries or older-artifact cleanup occurred. The root is retained for explicitly approved cleanup, not reuse. Source/dependency/query/binding aggregate remains `536230AD42EB27E0F9B56BA73CD511F672A703E0074BDB70D84B7DE3BF4F7AA8`; both older campaign report pairs match their original hashes. No source changes or additional app/build/startup tests were made for this execution-only package; prior full regressions remain historical verification.

Artifacts under `src-tauri/target`: `startup-sql-ownership-7821d90c-dabb-4b3b-a3dd-9e9af6b8ee5b.jsonl` and controller companion, plus matching `ownership-template-link` and `ownership-template-postflight` records. Worker SHA-256 `1c85e9a55655a82d449ac1790de7a5c75579b566bd7c6e25e897d0228d64a6bd`; controller `6d431a26668aa6796df24972968b360251d8678c0f14da2350873553749ce461`. Preflight linkage and generated-volume selection were independently checked.

Decision: stop this campaign at incomplete evidence. No automatic rerun or performance fix. A separately approved plan should revisit the experiment budget rather than add diagnostics or weaken the six-block gate; a 60-minute cap for the unchanged matrix is a possible next proposal, not authorization or a completion guarantee. Do not pool partial campaigns. Ownership contribution, real startup targets, catch-up and historical black-window cause remain unresolved; no owner testing requested.

### Latest completed package: reuse validated ownership templates

Owner approved the follow-up recommendation on 2026-09-06. Scope is test-only implementation and generated correctness verification, not another measured campaign. Prepare each of the six existing ownership conditions once, before measured reads, in the existing first-block condition order. Each template retains its own guarded generated directory for the entire run; each block/boundary still gets a fresh copied catalog and plugin registration. Preserve the existing queries, scope/bypass correctness checks, analysis/checkpoint behavior, connection treatment, deadlines, sample count and measured order. Do not read or reuse the two directories left by the timed-out campaign.

Acceptance: the actual campaign preparation seam creates six templates rather than 36; repeated use preserves template contents and identical results, mutations in a disposable copy cannot affect templates, and failure/panic unwinds all admitted template scopes through existing guarded cleanup. The controller verifies the new fixed preparation policy, six ordered template records and 36 correctly linked fixture admissions before accepting completion; historical reports remain readable and primary failure details survive incomplete evidence. Verify bounded reports, isolation, ownership/privacy fixtures, cleanup and result parity using small generated data, followed by regressions and independent safety/specification review.

Trade-offs: retain up to eight live catalogs (base, six templates, one measured copy), approximately 20 GiB at observed catalog sizes before transient sidecars/scratch. A future large campaign needs a fresh D-backed root and at least 32 GiB available as a conservative allowance, rechecked before launch; the old 16 GiB allowance is not reused for this higher-footprint design. Preparation moves ahead of all measured arms, so the new `prepared-once-per-condition-v1` policy must not be pooled with historical per-block preparation results. No wall-time improvement or 45-minute feasibility is claimed from small correctness tests alone. Normal app:dev remains tracing-disabled; no shipping API/schema/query/index/policy, dependency, library or credential changes.

Implemented: six guarded templates remain alive across all blocks; 36 condition admissions still produce 72 fresh boundary copies/registrations. The controller requires six ordered template records and valid admission links, rejects incomplete or malformed new-policy evidence, and retains the original failure classification alongside incompleteness. Template confinement is revalidated immediately before every arm copy. Cooperative deadline checks bracket template preparation and the final measurement handoff; error/panic cleanup clears cached template references after guarded scopes unwind.

Independent safety/lifecycle and specification/evidence closure are clean. Review findings corrected: TPL-1 recognizes new protocol bodies even with missing headers; TPL-2 preserves primary failures while marking missing template evidence incomplete; TPL-3 revalidates long-lived copy sources and tests contaminated-template rejection before IPC; TPL-4 checks preparation deadlines before admitting further work. No measured campaign was run. The historical preparation failure and ownership contribution remain unresolved; this implementation reduces repeated harness preparation, not shipping startup work.

Verification: three focused template tests pass; feature-enabled full native regressions pass 1,016 library and 18 utility tests, with 32 opt-in/benchmark tests ignored (`template-reuse-native.log`, 327.35s library suite). The subsequently added contaminated-template call-site test passes separately (`template-reuse-safety-closure-final.log`); its initial assertion-helper compile failure is retained in `template-reuse-safety-closure.log`, not counted as a pass. Frontend regressions pass 3,502 tests with one skip across 290 files; typecheck, lint, controller self-tests/syntax, scoped Rust formatting and whitespace checks pass. Shipping/dependency/binding/query protection aggregate and both historical campaign report pairs match their original hashes. No bindings regeneration, default-feature full suite, production frontend build or desktop build was performed for this test-only change; no release or startup acceptance is implied. Generated verification used a separate process-local D: temp root; older campaign directories were not accessed or cleaned.

Decision: implementation gate complete. Recommend a separately approved single campaign using the revised preparation policy, fresh generated fixtures and the stated space/deadline gates. Until then, ownership's contribution and full-campaign feasibility remain unmeasured; no additional owner launches are requested.

### Latest stopped package: run the diagnosed ownership comparison once

Approved 2026-09-06: reuse the existing six-condition generated-only ownership campaign with its verified preparation diagnostics. Preserve 288 rounds / 1,152 calls, six counterbalanced blocks, unchanged queries and uniform-default treatment, 45-minute campaign budget and 60-second round deadline. Require focused verification and 16 GiB free on the generated-fixture volume before the one new run; no retries, automatic relocation or deletion of older artifacts. Deliver independently reviewed complete comparisons or an explicit blocker, never partial ownership attribution.

Initial preflight stopped before tests or measurement: C: had 3,302,043,648 available bytes (3.075 GiB), below the approved 16 GiB conservative allowance. The owner then explicitly approved moving only generated fixtures to D:. No code change was needed: a fresh ordinary `D:/Ambit-Generated-Tests-c50ec004-37cb-4068-bdc1-5a7fa5765124` directory became TEMP/TMP only in transient generated-test/controller processes. Native generated children were observed there before measurement; the existing direct-temp-child and reparse guards remained active. Reports stayed on C: in the existing artifact directory. A subsequent normal process still resolved temp to C:. No persistent environment, app profile or library change occurred.

Verification before measurement passed: 13 ownership/preparation tests, 2 generated cleanup tests including real junction rejection, 7 overlap/settings/correlation tests, controller self-tests/syntax and scoped formatting/whitespace. Campaigns remained ignored during those tests. Cargo no-run confirmed the current feature test executable, SHA-256 `6c7cb3c0798c5cd54f58c24342cd35d360718609e636c3ec29de69788e4ba81e`. No source changes were made; full frontend/native regressions, typecheck/lint and desktop builds were not repeated for this process-local relocation. Prior full-suite results remain historical, not new verification. Independent safety review accepted the confined process-local setup; evidence review required and verified a linked immutable D-volume preflight record.

Single-run outcome: campaign `91b3d24c-02e3-40ed-a6e7-ea006283a7da` stopped at the controller's 45-minute deadline (2,700,080.4115ms), with `timed-out` / child `incomplete`. The controller requested termination of only its generated worker; no timed round was pending. Completed 128/288 rounds and 512/1,152 calls: blocks 0 and 1 cover all six conditions, block 2 covers All-users shipping/bypass, local bypass and selected-owner shipping. The next local-shipping fixture was interrupted at last-known stage `ownership-assignment`; that stage is not a reported error or proven cause. The previous setup failure did not recur, but D-backed success does not explain the historical C-backed failure.

Evidence is valid but partial: 1,718 contiguous worker records / 213,470 bytes, 512 unique exact IPC results matched one-to-one with 512 native timings, 16 admitted fixtures and paired boundary checks, 64 pool snapshots. No six-block summaries or ownership-penalty decisions were emitted. The worker has no terminal after forced termination; the controller retains the timeout. Independent evidence review confirms the incomplete classification, correlations, equality and volume/executable linkage. Do not pool these D-backed samples with historical C-backed measurements or infer ownership's share of real startup time.

Cleanup: 48 distinct generated scopes recorded `removed`; base and interrupted active scopes have unobserved cleanup. Post-run metadata inspection found two ordinary generated directories remaining in the approved D: root. They and the containing root were retained, without manual deletion or retry. No older artifacts were cleaned. Source/dependency/binding/query aggregate remained `E67546F979DBEEA28F240AEE528D97DB77FEF3D1215E0FAB9332195DA4375F1E`; historical failed reports still match their original hashes.

Artifacts under `src-tauri/target`: `startup-sql-ownership-91b3d24c-02e3-40ed-a6e7-ea006283a7da.jsonl` and controller companion; worker SHA-256 `e2d534e11ae77bcc319be980e7e6671f39b8783e797d91ce72301d605cf8d81e`, controller `e2fe7212dccc6c213fe4935097c6044f519bb4d5ec57eec7908b0015a40b6bac`. `ownership-d-preflight-c50ec004-37cb-4068-bdc1-5a7fa5765124.json`, `ownership-d-link-91b3d24c-02e3-40ed-a6e7-ea006283a7da.json` and the matching postflight artifact seal process-local volume selection, checks, report linkage and remaining-directory observations without private paths in evidence.

Preparation feasibility finding, independently reviewed and recalculated: completed stage-entry-to-next-stage wall spans total 599.362s for ownership assignment (17 spans, median 45.551s, maximum 54.977s), 889.604s for equality checks (17, median 51.885s, maximum 56.091s), and 414.797s for copying (49, median 8.610s, maximum 17.142s). Together these observed spans occupy about 31.7 minutes; initial fixture generation additionally spans 122.982s. These are preparation wall intervals, not pure SQLite operation timings, and exclude the final unclosed stage. They explain a campaign-budget problem, not Ambit's startup costs.

Decision: stop at incomplete evidence; no rerun or shipping optimization. Recommend a separately approved test-only feasibility revision investigating reuse of validated condition templates instead of repeated ownership assignment/equality preparation, while preserving fresh measured copies and correctness/privacy guards. This workflow change is only a proposal, not implemented or qualified. Preserve the original failed gates and black-window uncertainty. No owner startup batch is requested. Normal development remains tracing-disabled; latest real readiness/catch-up remains 37.422s/57.076s, with targets unaccepted.

### Previous completed package: make fixture-preparation failures diagnosable

Approved 2026-09-06: address OWN-SETUP-1 with fixed preparation stages, safe SQLite/OS numeric error codes, last-known stage on panic/interruption, and guarded generated-directory cleanup outcomes. Keep the original failure status and append structured detail; never persist raw errors, SQL, paths or identities. Preserve create-new ordered 1MiB reports with terminal reserve. The controller must validate and propagate only the fixed-shape detail, not arbitrary stderr.

Verification uses tiny generated databases and injected failures, including a deterministic red/green regression for the existing loss of setup detail. Check operation ordering, equality, cleanup safety, primary-error preservation, storage failure, partial/malformed evidence and redaction; run focused/controller/native regressions and independent safety/evidence review. No new campaign, owner batch, real-library access, shipping code, dependency, query or migration changes. The historical setup failure remains unexplained; a new measured campaign requires separate approval.

Implementation: one preparation workflow serves diagnosed and undiagnosed generated fixtures. Base, condition and arm directory scopes record cleanup after resources unwind on success, returned errors and panics, without retrying deletion. Diagnostics retain legacy status plus fixed stage/category and available SQLite/OS codes. SQLite syntax-error codes are retained without their SQL/message fields. The report declares preparation protocol v1; elapsedMs is campaign elapsed time at observation, not pure operation time. Interrupted/panic evidence records the last-known stage, not a proven cause. Dropped/storage evidence cannot produce a successful campaign and completeness remains explicit even alongside a primary setup failure.

Review ledger: PREP-1 (P2, corrected) rejects orphan condition/block fields. PREP-2 (P1, corrected) rejects non-directory, symlink and Windows reparse roots before cleanup. PREP-3 (P2, corrected) preserves completeness/drop/storage flags in controller output, requiring them for new structured/versioned reports while accepting historical reports. PREP-4 (P2, corrected) retains SQLite errors through scope, validation, bypass and equality helpers, including SqlInputError. Independent safety and specification/evidence closure are clean; all findings are verified.

Verification passed: controller red/green test demonstrates the previous loss of setup detail; 13 focused ownership tests pass with the opt-in campaign ignored; generated preparation tests cover six identical-output conditions, stage ordering, real copy/SQL failures, panic/cleanup, saturation and unavailable storage. The real Windows junction test passes without symlink privilege. The first full native run found one new error-variant regression (1,013 pass, one fail): syntax-error codes were omitted. The unchanged regression passes after extracting the numeric code from SqlInputError; the failed log is retained at `src-tauri/target/prep-diagnostics-native.log`. Final native closure passed 1,014 library + 18 utility tests, with 32 opt-in/benchmark tests ignored, recorded in `src-tauri/target/prep-diagnostics-native-closure.log`. Frontend 3,502 tests / one skip, typecheck, lint, controller self-tests/syntax, scoped Rust formatting and whitespace checks pass. No production frontend/desktop build or full default-feature native suite was rerun for this test-only package; default-feature focused cleanup/junction tests passed. Bindings were not regenerated because no shipping interface changed.

Scope verification: changes are confined to the ownership campaign/controller, shared cfg(test) directory/count-read helpers, and these plan/progress notes. Shipping code, dependencies, bindings, queries and migrations are unchanged; pre-existing dirty work is preserved. Both historical failed-campaign JSONL hashes still match their original records. No campaign, app launch, library access/copy/import or owner startup batch occurred. Future reports retain their existing create-new location under `src-tauri/target/startup-sql-ownership-<id>.jsonl` and the controller companion; this phase created no new campaign report.

Decision: OWN-SETUP-1 is addressed as a diagnostic capability, not a retrospective explanation or performance fix. Legacy helper panics retain only their last-known stage and unknown cause; forced termination cannot guarantee cleanup or a terminal record. The historical preparation failure, ownership contribution and startup-speed acceptance remain unresolved. Any new measured campaign requires separate approval; normal development remains `pnpm run app:dev` with tracing disabled.

### Previous completed package: measure ownership's contribution to count delays

Approved 2026-09-06: one generated-only campaign comparing local, InvokeAI All users and selected-owner count paths, each with a test-only bypass control. The owner reports fast startup without InvokeAI images; the suspected v0.11.1 regression is recollection, not verified release attribution. Prior overlap experiments did not isolate ownership cost.

Use the existing wide 146,182-image / 131,741-membership fixture and four unchanged count queries, with local collections and identical non-ownership content. All InvokeAI rows belong to one generated source/owner so all six timed conditions return identical results. Local conditions have no source/owner/current scope. Bypass only the three scoped views in guarded disposable generated databases, retaining column shape and collection suppression; never expose it to the app. Small 10k correctness checks additionally reject mixed-owner/other-source output divergence. Record physical-size differences rather than attributing cross-catalog latency solely to predicates.

Six Williams-order blocks ([0,1,5,2,4,3] shifted by block modulo six), alternating sequential/concurrent boundaries and sequential query order, use fresh copies/registrations: one initial plus three warm rounds each, 288 rounds / 1,152 calls. Reuse the approved all-ten uniform-default treatment and before/after lifetime/settings checks. Allow 45 minutes including fixture preparation, excluding compilation/regressions, with the existing external 60-second round and tracing deadlines. Stop on errors, timeouts, incomplete traces, mismatched results/settings or replacement; preserve partial evidence without timing-driven reruns.

Retain create-new 1MiB bounded evidence with terminal reserve, provenance/control hashes, fixed labels, exact equality and separate IPC/native boundaries. Compare shipping versus bypass within each condition, All users versus local, and selected owner versus All users; use median of three warm reads per block then six-block median. A material repeatable penalty requires both 20% and 200ms, with matching direction in five of six blocks. Report full four-query native span separately. Smaller effects are not absence; generated timings are not historical startup attribution, source-sync or multi-owner-cardinality acceptance.

Before measurement: focused isolation/confinement/equality, median/group-span, ordering, lifetime/settings, malformed/missing/duplicate/partial evidence and capacity checks; native regressions, controller tests, formatting/whitespace and independent concurrency/isolation and specification/evidence review. Shipping files/dependencies/bindings/query/schema fingerprints must remain unchanged. Deliver a separately proposed narrow follow-up or explicit inconclusive result. No library access, app launches, imports, owner testing, real bypass, new indexes, shipping tuning or scheduling changes. Latest real readiness/catch-up remains 37.422s/57.076s. Previous gates and black-window uncertainty remain preserved.

Outcome (2026-09-06): **inconclusive; the single campaign stopped with `fixture-setup-failed` after 691.521s** (controller 691.611s). It completed 24/288 rounds and 96/1,152 exact IPC results with one-to-one native records. Only block 0 local shipping, local bypass and selected-owner bypass completed, sequentially and concurrently. The next Williams-order condition was InvokeAI All users with shipping views; it failed before its fixture admission record or timed reads. No All-users shipping or selected-owner shipping timings, six-block comparison, or material-penalty decision exist. Do not use these partial observations to quantify ownership cost or recommend a performance change. No rerun occurred and no worker termination was requested.

Retained evidence: `src-tauri/target/startup-sql-ownership-c0bcc481-0a55-44aa-aed7-5504e934ff86.jsonl` and its `-controller.jsonl` companion. The worker report is 33,624 bytes with 260 contiguous records and an explicit failure terminal. All 12 completed pool snapshots have ten uniform-default settings; all six completed arms retain stable lifetimes, zero closed-original/marker counts, and three equal boundary-setting comparisons. Completed local fixtures were 2,700,873,728 bytes; selected-owner bypass was 2,706,157,568 bytes. These sizes do not establish the cause of failure.

Provenance: revision `8e4bed6d222c517a4416ddfcf6ce2174e937bd6a`, verified executable SHA-256 `0e1475288e8c5c8c4b81f587a41e0dd6f4da7ac04aacd83a58955c3f90f95e1a`, harness `fbe5abfe8c89a82e04ae560cb69e2a633e911fbc820f2f44a9db36bd73ec4ff3`, controller `d3b1b7c6ab4f48cb2439db87dc33dcb44076ed03d5a4e40bd6df7165c0061867`, bypass control `eb56bd0c307d0276458fb514228e035d3c036faf129585f4d2386b5ae3c96632`. Existing fixture, query, vendor, adapter and journal hashes match the preceding evidence. Protected shipping/source/dependency/binding aggregate before and after is `74292D42CBE2F755BD1193182A2FAC81BFF7D6050F54A435BB2EF38237CAAD6A` (excludes only the test-harness folder).

Verification: baseline overlap checks passed; full feature-native serial regressions passed 1,007 library + 18 utility tests, with 32 opt-in tests ignored. Frontend passed 3,502 tests / one skip; typecheck, lint, controller deadline/status/size tests, scoped Rust formatting and whitespace checks passed. Generated tests cover all six conditions through actual MockRuntime IPC, exact typed content/membership hashes, scope/source/owner mislabelling, confinement, divergent privacy fixtures, medians/group spans and complete report capacity/partial termination. An initial content-mutation test produced invalid JSON and failed; the original log is retained, and the valid same-length mutation/restoration fix passed both focused and full serial checks. Existing compiler warnings remain. Default-native, packaged desktop and frontend production builds were not rerun for this test-only change; bindings were not regenerated because no shipping interface changed.

Review closure: both independent code-review axes were clean before the run; independent evidence review confirms the stop, exact results, correlations, source hashes and redaction. Follow-up finding **OWN-SETUP-1 (P2, deferred)**: the fixed `fixture-setup-failed` classification combines ownership update, connection opening, analysis/checkpoint and metadata failures, so the retained report cannot identify the failed operation or safe error code. Cleanup failures are also stderr-only. Disk snapshots changed during/after the run, but neither disk exhaustion nor a lock/cleanup failure is established. Read-only metadata inspection found no generated-benchmark directories created within this campaign's recorded time window remaining in the configured temp root; older verification artifacts were not removed. This is a post-run observation, not complete cleanup telemetry.

Decision: stop this approach at incomplete evidence. Recommend a separately approved, test-only follow-up recording bounded setup-stage/error-code and cleanup outcomes before authorizing any new measurement campaign. Do not add a performance fix, reinterpret the failed run, or request owner launches. Ownership's contribution, historical startup delay and black-window cause remain unquantified; earlier startup improvements and failed experimental gates remain intact.

### Previous package: isolate overlap with uniform generated connections

Approved continuation 2026-09-06: reuse the existing campaign under a distinct explicit opt-in and create-new report identity. In both arms, run the unchanged frontend setup, then hold all ten physical connections and set their connection-local values to the observed defaults: synchronous 2, busy timeout 5000ms, cache size -2000, temp store 0 and mmap 0. Retain the already established WAL; do not set journal mode during treatment. Record untreated and treated distributions plus setup duration outside measured count calls. Require ten exact default rows before and after measurements, the same physical-lifetime checks, and zero marker calls. Do not retune after treatment or mask replacement. This deliberately uniform condition is not shipping configuration, a memory optimization or a complete startup simulation.

Keep the two existing generated fixtures, All users, four unchanged queries, six counterbalanced pairs, one first plus three warm rounds, 30-minute preparation/measurement budget and 60-second external round deadline. One new campaign only after deterministic treatment tests, native regressions and independent concurrency/spec/privacy review. Preserve the prior invalid comparison; stop on timeout, mismatch, changed settings, incomplete traces or error without another treatment/campaign. Retain individual/paired timings, exact results, source hashes and explicit terminal status. Use the existing 20% plus 200ms / five-of-six penalty gate and report multi-second reproduction separately. No owner testing, real-library access, shipping settings/dependencies/query/schema changes or app launches.

Deliver a separately proposed mechanism-isolation step if the effect reproduces, otherwise an explicit nonreproduction or incomplete result. The historical startup remains unattributed; no automatic next optimization is authorized.

Outcome (2026-09-06): the single campaign completed all 96 rounds in 464.665s (controller 464.736s), with 384 exact IPC results matched to 384 native records. All 48 before/after pool snapshots had ten identical fixed-default rows, all 12 paired distributions matched, and all final lifetime checks had zero closed originals / marker calls. No timeout, retuning, replacement, record loss or rerun occurred. Report `src-tauri/target/startup-sql-uniform-overlap-5236d49e-b9c2-4272-be14-6de9019e0490.jsonl` is 124,617 bytes with 998 ordered records and a completed terminal; its controller companion retains executable SHA-256 `a860229d3a5f05cd317060d489a110c5e104273c57af053d7b6e8b5bf469338b`. The manifest records treatment `all-ten-observed-defaults-wal-v1`, revision and source/query/vendor hashes; untreated distributions and preparation time remain distinct from measured counts.

Paired warm retrieval medians (ms; median of each pair's three warm reads, then median of six pairs):

| Catalog | Query | Sequential | Concurrent | Penalty gate |
| --- | --- | ---: | ---: | --- |
| 10k | Collection | 20.096 | 37.610 | Not met |
| 10k | Maintenance | 162.336 | 221.515 | Not met |
| 10k | Gallery | 106.474 | 157.691 | Not met |
| 10k | Global gallery | 99.149 | 157.715 | Not met |
| 146,182 | Collection | 3070.751 | 4230.272 | Met |
| 146,182 | Maintenance | 2174.510 | 3613.728 | Met |
| 146,182 | Gallery | 1396.168 | 2417.267 | Met |
| 146,182 | Global gallery | 1396.702 | 2417.271 | Met |

All eight comparisons were slower in six of six concurrent pairs. Small-query additional latency stayed below 200ms. Large queries met both 20% and 200ms thresholds; this is a repeatable generated-workload overlap penalty. The slowest retrieval overall was the initial sequential collection read at 9496.721ms; maintenance peaked at 4758.143ms. No 15–19s call reproduced. Individual first/warm readings and slowest values remain in the report; these are not OS-cold or real startup measurements.

Important implication: slower individual calls did **not** mean slower completion of all four. Derived from the retained native intervals as `max(start + total) - min(start)` per round, then the same paired warm aggregation, the large four-count span was 8075.190ms sequential versus 4231.243ms concurrent. This derived group boundary includes inter-command gaps and excludes frontend delivery; it is not gallery readiness. It argues against blanket serialization based on individual call penalties alone. Independent review corrected an odd-length median indexing error in the first handoff calculation; raw campaign records and their built-in query medians were unchanged.

Decision: recommend a separately approved generated-only mechanism-isolation experiment identifying which query pairs compete, with both individual latency and complete count-group span as outcomes. Do not alter shipping scheduling from this result. CPU, storage, memory/SQLite-worker contributions and the historical 15–19s gap remain unseparated. No larger fixture, new tracing, connection policy, query/index rewrite or owner launch was added. Targets and latest real 37.422s readiness / 57.076s catch-up remain unchanged.

Independent evidence closure is clean after the derived-median correction: ordered records, exact correlations, uniform snapshots, zero marker/lifetime failures, controller completion and source/vendor provenance were rechecked. This closes the generated experiment, not startup-speed or reliability acceptance.

Verification: exact-treatment test failed before implementation and passed afterward; coverage verifies all ten held physical connections, unchanged exact results for all four queries, rejection of mixed/nine-row treatment, no silent retuning, stable lifetimes and report capacity including untreated settings. Feature-native suite: 999 library + 18 utility passed, 31 intentional ignores. Frontend: 3,502 passed / one skipped; typecheck, lint, controller syntax/deadline tests, scoped Rust formatting and whitespace passed. Independent standards/concurrency and specification/privacy code reviews are clean. Shipping/source/dependency/binding protected aggregate remains `E8E179AF3659B6F183985B6B7A795B7861EAE4AD77B3922866C3812106581AFA`; adapter hash and vendor/query hashes remain the preceding qualified recorder build's values. Default native and desktop/package builds were not rerun for this test-only change; no generated bindings changed. Previous failed gates and diagnostics remain preserved.

### Approved continuation: repair concurrent diagnostic publication

Approved 2026-09-06 after the failed qualification below: repair only the nondefault recorder and its frontend-report adapter, then continue through verification. Fixed, launch-lifetime slots replace producer access to shared observer storage. Limits, query execution, pool/registry lifetimes, dependencies and public payloads remain unchanged. Pending/incomplete component timings remain unavailable until terminal publication; reserved but unpublished slots remain explicitly unknown. This is diagnostic correctness, not a startup performance fix.

Gates: deterministic loss reproduction and concurrency/lifecycle regressions; independent concurrency/privacy review; one retained post-fix IPC qualification; the existing six-pair generated overhead screen for this changed build; only then the still-unrun single 30-minute overlap campaign. Stop on a failed gate, retaining partial evidence without timing-driven reruns. No app launches or real-library access. The original failed qualification remains historical failed evidence, not overwritten by this new build.

Outcome (2026-09-06): recorder repair qualified, but the one overlap campaign is **inconclusive at large scale**. It stopped after 178.14s / 52 completed rounds, before the first large concurrent read. The first large sequential arm had nine default connections and one with the complete legacy bundle; its paired concurrent arm had eight defaults, one mmap-only connection and one legacy-bundle connection without mmap. The unchanged pooled frontend PRAGMA calls therefore produced unequal treatment distributions. The gate correctly rejected that comparison; no retuning or rerun occurred. This observes setup nondeterminism, not proof that it caused the historical startup delay.

All six small-catalog pairs completed. Paired warm retrieval medians, sequential/concurrent (ms): collection 12.070/19.019; maintenance 119.677/143.853; gallery 88.457/117.632; global gallery 90.297/117.626. Every query was slower in all six concurrent pairs, but none met the required additional 200ms threshold; the slowest small reading was 168.957ms. No 15–19s delay reproduced. The unpaired large sequential arm ranged 1.218–3.046s across 16 reads; it is partial evidence, not a large-catalog overlap comparison. Across the campaign, all 208 IPC results were exact and matched 208 native records. No missing observations prompted this stop.

Decision: stop this branch with invalid large-catalog comparison evidence. A separately approved generated-only experiment would need a deterministic, explicitly labelled pool-setting treatment before isolating overlap. Do not silently fix/tune shipping connections, infer which physical connection ran a historical query, or serialize startup from this evidence. Real readiness remains 37.422s and catch-up 57.076s; no owner startup run is requested, and normal app:dev stays tracing disabled.

Independent final evidence review is clean for the stop and retained partial results: 539 ordered records, 208 unique exact IPC/native matches, all small-pair decisions recomputed, and no large concurrent readings. This is not campaign completion or startup acceptance.

Retained create-new reports in `src-tauri/target`, each with its controller companion: qualification `startup-sql-qualification-c2fa82e7-da53-4322-a4e4-184f386ed9fc.jsonl` (8 rounds, 32 matched exact counts); overhead `startup-sql-overhead-986c28e7-829e-4992-be90-d908c95c12bb.jsonl` (192 exact reads, 96 matched traced counts, all eight gates accepted); campaign `startup-sql-overlap-7e09047e-a7ae-429d-af6f-2850f7220059.jsonl` (66,633 bytes, explicit invalid-comparison terminal). All use executable SHA-256 `b7da858f9b9dea44cac5233fab813161aa1a5a3b5bf0abfb849193fc4dcdf7da`; source/vendor/query hashes are retained. The old failed preflight remains unchanged.

Verification: deterministic held-observer native test failed 0/4 before and passed 4/4 after; ten collector tests and six frontend-adapter tests pass. Feature-native regressions: 998 library + 18 utility passed, 30 intentional ignores. Default-native: 975 + 18 passed, 27 ignores. Frontend: 3,502 passed/one skipped; typecheck, lint, controller self-tests, scoped Rust formatting and whitespace pass. Independent concurrency/privacy and qualification/overhead evidence reviews are clean. Protected shipping/source/dependency/binding hash excluding authorized diagnostic files is unchanged at `E8E179AF3659B6F183985B6B7A795B7861EAE4AD77B3922866C3812106581AFA`. No binding regeneration was needed; no public types changed. Packaged/desktop builds and owner performance acceptance were not rerun for this diagnostic-only repair. Historical blank-window attribution and prior failed optimization gates remain unresolved/preserved.

### Campaign specification and original failed qualification

Approved 2026-09-06: one test-only sequential/concurrent comparison through actual MockRuntime SQL-plugin IPC on the existing unindexed wide 10k/1k and 146,182/131,741 generated fixtures, All users. Six counterbalanced pairs per size; fresh copies and registrations per arm; one first and three warm rounds of the four unchanged count queries. Preserve the existing frontend PRAGMA setup, plugin, dependencies, queries, schema and shipping scheduling. No real-library access or app launches. The latest changed-source readiness is 37.422s, catch-up 57.076s; image arrival is now owner-confirmed, second-run responsiveness remains unconfirmed.

Before and after each arm, hold all ten connections and compare effective settings distributions and physical lifetime continuity. Snapshots do not identify the connection that executed any particular count. Mismatched paired distributions, replacement, timeout, error or unequal results stop the campaign; no retuning or timing-driven rerun. Native retrieval remains inclusive of SQLite/worker/row-transfer costs, not pure execution. Record IPC and native phases, actual overlaps, individual results, first/warm medians and maxima. A repeatable overlap penalty requires at least 20% and 200ms extra warm retrieval, with the same direction in five of six pairs; report absolute multi-second reproduction separately.

Approved clarification: SQLx exposes no public nonmutating lifetime identifier, so raw handle equality cannot exclude address reuse. The owner authorized a generated-connection-only lifetime marker on 2026-09-06. An unused collation owns a drop sentinel; tests must establish that replacement drops the sentinel and the unchanged measured queries never invoke its comparator. Both arms receive the marker outside timing. This adds test setup state/very small retained allocation and may invalidate prepared statements at installation, before the first measured count in both fresh arms. It is not a shipping connection change or database-content write, and it is not evidence of zero observer effect. No other deeper SQLx instrumentation is authorized.

Reuse the existing deadline controller: 30 minutes including generation, 60 seconds per measured round, terminating only its own generated worker. Preserve incomplete evidence and stop if the existing 180-second trace lifetime prevents a complete arm. Bounded create-new JSONL retains fixed provenance/settings/timings/equality/status, never SQL, identities, paths or raw errors. Focused tests, native regressions, formatting/whitespace, shipping-integrity checks and independent concurrency/isolation and evidence review precede the single campaign. Outcome must be a separately proposed next experiment or explicit nonreproduction/incomplete result; no optimization is authorized. Targets remain under 10s warm and 30s changed-source readiness, unaccepted.

Pre-campaign outcome: the synchronized generated IPC smoke failed with incomplete timing evidence. A diagnostic verification rerun added bounded failure classification rather than changing scheduling or the recorder: 12 count records were expected, 11 observed, 11 detailed admissions, 6 detailed-drop observations, 6 coarse-drop observations and zero frontend drops. Drop counters are not counts of uniquely missing queries. The installed collector uses nonblocking shared-lock admission/publication and can discard observations under contention; the counters do not identify the exact dropped stage. This is an evidence-collection limitation, not proof of a query failure, missing images or the cause of slow startup.

The original generated journal was removed by fixture teardown. Retained failure notes are explicitly a post-hoc transcription of the implementation agent's captured test output, not a revalidated original journal or an exact failed-build manifest. The first smoke failure and the classified diagnostic rerun remain failed gates; neither is cleared by unrelated passing tests. The actual-concurrency smoke is an explicit opt-in qualification gate, not an ordinary deterministic regression. Missing/drop validation, marker lifecycle, dispatcher, settings and capacity tests remain active.

Decision: **inconclusive; the 30-minute campaign was not run**. No sequential/concurrent latency comparison or startup improvement is established. Preserve this failed gate and do not rerun qualification for favorable loss, stagger calls, weaken completeness or patch the collector within this scope. A separately approved diagnostic-recording change would be needed before renewed concurrent qualification; it would not itself be a startup optimization. No owner launch is requested. Normal app:dev remains tracing disabled; the lifetime marker remains generated-test-only.

Verification/handoff: six deterministic focused tests passed; default native 975 library + 18 utility passed (27 intentional ignores), feature native 997 + 18 passed (30 intentional ignores, including the explicitly failed qualification and unrun campaign). Frontend 3,502 passed/one intentional skip; typecheck, lint, controller deadline/evidence self-tests, scoped Rust formatting and whitespace checks passed. Independent standards/concurrency/isolation and specification closure reviews are clean for this stopped handoff, not campaign approval. Existing ComfyUI dead-code and line-ending notices remain. Shipping/source/dependency/binding aggregate SHA-256 is unchanged: `81D21B7C2E233946BD6DCAEC3A83125D8E54B33F8F0EF70B88868C8356F96589`. No desktop build, app launch, library access or benchmark campaign occurred in this phase.

Retained artifacts: `src-tauri/target/startup-sql-overlap-preflight-0ff490d2-a2f4-4790-8ee8-dbd5ef62fad5.jsonl` is the explicitly post-hoc failure transcription; `overlap-default-native.log`, `overlap-feature-native.log` and `overlap-frontend.json` in the same build-artifact directory contain regression results. Changes are limited to the existing test harness, existing generated-worker controller, this plan and progress notes. The generated-only marker is installed before measured reads in both arms, never invoked by the unchanged count queries in its lifecycle regression, and does not affect normal dev/prod connections. It is diagnostic test setup, not a performance improvement.

### Previous package: locate startup count delays inside the SQL plugin

Approved 2026-09-05: opt-in regular-dev diagnostics, not a performance patch. Preserve the latest real 37.159s readiness / 99.306s catch-up and direct read-only maximum 2.244s as different measurement boundaries. Do not infer lock waiting by subtraction. Targets remain under 10s warm and 30s changed-source readiness; neither is established by this package.

Vendor installed SQL plugin 2.4.0 through a local Cargo patch with nondefault `startup-sql-trace`. Default execution remains upstream; dependency provenance changes even when tracing is off. Trace builds are debug-only and reject identifiers other than existing `com.ambit.dev` before normal startup work. The owner command is `pnpm run app:dev:sql-trace`; ordinary `app:dev` stays tracing-disabled. No additional profile, queries, PRAGMAs, indexes, migrations, connection policy or scheduling changes.

Correlate four count families across the frontend/native boundary using fixed labels and launch-local call IDs. Malformed/foreign/duplicate plugin metadata disables diagnostics, never the query. Separate registry acquisition, parameter binding, pool acquisition, held-connection retrieval and unchanged JSON conversion; preserve pool-return and registry-guard lifetimes. Acquisition includes opening/health checks; retrieval includes SQLite scheduling/locking/preparation/rows; conversion excludes response serialization/delivery. Coarse plugin intervals and existing startup phases show overlaps, not causes.

The existing journal gets a 64KiB allowance carved from ordinary capacity for 32 detailed counts and 128 coarse operations, preserving the existing 32KiB terminal and 8KiB repair reserves within 256KiB/20-launch limits. SQL callbacks only update bounded memory without blocking or disk writes. Stop admission at ready/failure/180s, retain admitted completions, and distinguish dropped/incomplete/unmatched evidence. UUID-only build identifiers link to create-new source/vendor/query evidence manifests. No SQL, parameters, results, owner identities, library paths or raw errors are retained.

Verification: 3,502 frontend tests passed with one intentional skip; default native 975 + 18 utility tests passed; trace-enabled native 988 + 18 utility and 5 plugin tests passed. The final focused gate passed 10 IPC/journal + 5 collector tests, including added controlled retrieval delay, migration-failure parity and ready-to-deadline regressions. Typecheck, lint, binding drift, scoped Rust formatting, whitespace and guarded frontend output passed. Independent concurrency/privacy and specification reviews are clean. Serial default release and diagnostic debug desktop builds passed without launching either application. Release tracing was rejected by the intended compile-time guard. Existing ComfyUI dead-code warnings are unchanged.

The first and only generated-wide-10k overhead campaign completed in 9.038s: `src-tauri/target/startup-sql-overhead-1a1d67b8-fc32-47f8-b8b6-221fa77a0108.jsonl` and its separate deadline-controller report. Six counterbalanced pairs produced 192 equal results, 96 matched traced counts and zero reported drops; all eight gates passed. First-read baseline/traced medians (ms): collection 6.644/6.682, maintenance 28.444/29.920, gallery 9.857/10.152, global gallery 21.843/22.260. Warm medians: 4.383/4.531, 8.995/9.216, 7.218/7.259, 6.373/6.386 respectively. Four query hashes match the previous read-only probe. This measures generated MockRuntime IPC with the live journal observer, not renderer/report IPC overhead, OS-cold startup or owner performance. No timing-driven rerun occurred. Independent evidence closure is clean.

Review closures: canonical UUID validation prevents build-environment strings entering logs; frontend/native status consistency prevents residual arithmetic on incomplete evidence; a separate generated-worker controller enforces the 20-minute deadline even if IPC stalls; only the observer consumes collector records, with overhead validation performed against the persisted journal; ready-to-180-second unmatched evidence remains unknown until its explicit deadline. No optimization was introduced to satisfy these gates.

After those gates only: two owner-controlled All-users launches in regular dev, first without deliberate source changes, then after closing normally and generating eight Odin images. Read each journal before continuing; owner reports responsiveness and image arrival, not stopwatch timings. A blank window stops the batch after approximately 60s observation. No forced repair/repeated attempts. Deliver a separately approved narrow optimization proposal or explicit inconclusive result, then use normal tracing-disabled development. Historical black-window/full-repair attribution and catch-up remain separate.

The approved two-launch owner batch is complete; its matched evidence follows. No further owner launch is requested. Use ordinary `pnpm run app:dev` for subsequent development, with tracing off. Retained launch journals remain under `C:/Users/Artemis/AppData/Local/com.ambit.dev/logs/startup/`; vendor removal still requires the reviewed procedure in `src-tauri/vendor/tauri-plugin-sql/AMBIT_DOWNSTREAM.md`.

First owner launch (2026-09-05): launch `744d7044-299f-4c4f-9533-4a75b7e1fd4a`, build `c3ad36ef-ea35-44ae-9760-aeffdf79192d`, revision `8e4bed6d222c517a4416ddfcf6ce2174e937bd6a`; matching source/vendor manifest retained under `src-tauri/target`. Owner confirms the gallery appeared and remained responsive, with the app left open. Admission was actually **unchanged / All users**, compatible snapshot, supported/current fingerprints, equal count/timestamp comparisons and no forced refresh. Native-boot boundaries: splash 26.833s, first safe page 28.228s, readiness 28.376s, catch-up completed 40.102s. The readiness summary correctly predates catch-up; its earlier “not recorded” is superseded only by the later catch-up event, not rewritten.

All four detailed counts matched; 61 coarse observations settled; all dropped/pending/storage-drop counters were zero. Frontend/native-fetch milliseconds: maintenance 15903/15901.499, collection 12103/12098.649, gallery 1998/1879.733, global-gallery 2006/1888.052. Registry acquisition was 0.007–0.019ms and pool acquisition 0.034–0.057ms; JSON conversion was at most 0.967ms. Matched dispatch/response residuals were 1.376ms maintenance, 3.312ms collection and approximately 118ms for each gallery count. Maintenance/collection overlapped from approximately 10.1s after native boot; overlap alone establishes no cause. For these calls the dominant boundary is **retrieval after acquisition**, not registry/pool acquisition or JSON conversion. Retrieval still combines worker scheduling, preparation, execution, SQLite locking and row transfer, so pure execution or lock waiting remains unattributed. No WebView failure was recorded. This is one diagnostic launch, not performance/reliability acceptance; historical changed-source 37.159s/99.306s remains a separate baseline. The completed second launch is recorded below; no further owner batch is requested.

Second owner launch (2026-09-05): launch `4b3b8ded-535d-44fa-a1f2-a77057e6c423`, build `3ac221e9-a6b8-4ff5-ae90-891bc7911f6b`. Revision, all manifest source hashes and vendor hash exactly match the first run. This was the requested eight-Odin-image exercise; image arrival is now explicitly owner-confirmed; visual responsiveness for this second run remains unconfirmed. Admission actually selected **incremental-catch-up / All users**, with increased count/timestamp comparisons, a compatible snapshot, supported fingerprints and no forced refresh. No full repair ran. Native-boot boundaries: splash 35.572s, first safe page 37.268s, readiness 37.422s, catch-up completed 57.076s. React mounted at 12.201s (first run 7.775s); count timings do not explain that earlier interval.

All four detailed counts matched; 62 coarse operations settled, with zero dropped/pending/storage-drop counters. Frontend/native-fetch milliseconds: maintenance 19378/19375.718, collection 14563/14557.206, gallery 2263/2155.065, global-gallery 2251/2143.511. Registry acquisition was 0.009–0.017ms and pool acquisition 0.037–0.077ms. JSON conversion was at most 1.550ms; matching dispatch/response residuals were 2.149ms maintenance, 4.175ms collection, and approximately 108ms per gallery count. During the overlapping maintenance/collection interval, recorded plugin operations were these reads plus three short main-database selects; this does not observe every native/external database operation or establish causation. No WebView failure was recorded.

Batch decision: expensive count retrieval reproduced in both launches, but the precise cause remains **inconclusive** within the measured boundary. These count calls did not spend their seconds acquiring the plugin registry or pool, binding parameters, converting JSON or crossing frontend/native transport. A registry-lock change is not supported as their remedy. Next planning should target maintenance/collection retrieval specifically, obtaining a reproducible distinction between SQLite preparation/execution, internal busy/lock waiting and SQLx worker scheduling before selecting a fix. Do not reopen failed index/tuning gates or subtract standalone probe timings. No further owner launch is requested. Return subsequent normal development to `pnpm run app:dev` (tracing off), with no automatic close/relaunch or removal of retained evidence. Targets remain unmet/unaccepted; this batch is not speed or reliability acceptance. Second-run image arrival is confirmed; responsiveness remains unconfirmed, with no additional owner testing requested.

### Previous package: support closed-WAL probing and obtain count timings

Approved 2026-09-05: permit SQLite-managed WAL/SHM coordination files beside the exact regular-dev database, while preserving READ_ONLY/query_only, the two-second lock timeout, path/reparse checks and app/dev-runner closure. This explicitly supersedes only the prior missing-sidecar refusal. No writable DB open, immutable mode, manual sidecar creation/deletion, checkpoints, permissions, production or shipping changes. Reject rollback journals and unexpected filesystem objects before opening.

The main database stamp stays fixed. A pre-existing WAL must remain unchanged; an initially absent WAL may appear only empty and disappear through SQLite lifecycle. SHM lifecycle/bookkeeping is permitted only for regular nonredirected files. Per-connection data_version and exact-result checks remain; connections close before final checks. No rebasing of existing-data evidence. Record fixed sidecar-policy/presence/transition classifications in a fresh bounded report and retain the earlier failed report.

Generated-data gates cover closed-WAL/no-sidecar, existing empty/nonempty WAL, missing SHM, byte preservation, write refusal, external commits, redirection and unexpected transitions, plus prior deadline/redaction tests. Preserve four unchanged queries, initial reads and three sequential/concurrent rounds, 15-minute budget and 60-second query deadlines. Stop rather than weakening a failed guard or rerunning. The latest readiness remains 37.159s and catch-up 99.306s, with image arrival confirmed; this phase has no new startup acceptance or real count results yet.

Implementation and verification complete: policy v2 allows only the approved transitions, and reports before-open, after-open and final sidecar presence, including unavailable failed teardown. The original error remains authoritative when final observation is unavailable. The CIM preflight checks the app and dev runners without outputting process arguments and fails closed on unavailable arguments. Focused tests passed 16/two opt-in ignores; native regressions passed 975 library plus 18 utility/27 opt-in ignores; frontend passed 3,495/one intentional skip. Typecheck, lint, Rust formatting and whitespace checks passed. Independent safety/concurrency and specification reviews closed clean. One medium finding (missing policy version in failed-teardown evidence) was corrected and regression-tested. Existing dead-code, jsdom-navigation and CRLF notices remain.

Only the test-only campaign and these notes changed in this phase; tracked shipping fingerprints match the pre-change aggregate `E90B4AB3732F77CB25DF02331F923A07195AAA291890578DCE38CBB15223B325`. Dependencies and generated bindings were unchanged; no app builds/launches, library copies, production or credential operations were performed. Logs: `src-tauri/target/count-probe-sidecars-focused.log`, `count-probe-sidecars-native.log`, and `count-sidecar-frontend.json`. The previous failed campaign report is retained.

Real campaign completed once after owner-confirmed dev-runner closure and a clear fixed preflight. Report: `src-tauri/target/count-read-only-1336ebef-4a22-4725-b010-b557caac4ba0.jsonl`. Test execution took 30.08s, not an Ambit startup measurement. All 49 JSONL sequence numbers, 28 unique reads (four initial plus 24 repeat reads), repeat exact-result checks, eight summary medians/maxima and completed terminal were independently read back and validated. Main/WAL metadata and data_version checks passed through teardown. A nonempty WAL and SHM already existed before this invocation and remained afterward; no manual cleanup occurred. This real run therefore exercised existing sidecars; closed/no-sidecar compatibility is established by generated tests, not this run.

Measured direct read-only query latency in seconds (initial reads are not OS-cold):

| Query | Initial | Sequential median | Concurrent median | Slowest read |
| --- | ---: | ---: | ---: | ---: |
| Collection | 1.267 | 2.212 | 1.813 | 2.244 |
| Maintenance | 1.403 | 1.398 | 2.157 | 2.178 |
| Default-visible gallery | 0.623 | 0.839 | 1.189 | 1.190 |
| Global gallery | 0.607 | 0.604 | 0.966 | 0.979 |

All four connections reported SQLite 3.46.0, query_only=1, WAL, busy_timeout=2000, cache_size=-2000, mmap_size=0, synchronous=2, temp_store=0 and page_size=4096. These are probe settings, not effective settings of the app's pooled connections. Opening plus read-only policy took 0.160–0.536ms per connection. Default-visible gallery is not proof of the owner's historical filter state. Collection results returned 377 aggregate rows; private result contents were not retained.

Conclusion: historical 11–15s application count-call durations did not reproduce here; no direct query exceeded 2.244s. This is useful isolation evidence, not attribution of the difference to locks, a query optimization qualification, or startup acceptance. The next proposed scope is measurement of the actual application SQL-plugin count-command path and overlapping work, to distinguish waiting from query execution before changing queries, indexes or scheduling. No implementation of that proposal is authorized by this campaign. Do not subtract these durations from earlier launches to estimate lock waiting or reopen failed tuning/index gates. No retries or further owner startup batch are requested; normal app use may resume.

### Previous package: isolate gallery-readiness count costs

Approved 2026-09-05: extend the existing opt-in read-only probe and perform one bounded regular-dev campaign with Ambit closed. No shipping changes, real-library copies, InvokeAI source access, tuning, migrations, indexes or automatic launches. Reject unexpected or redirected database targets. Four unchanged queries cover collection counts, maintenance counts, default-visible gallery count and global count. The default-visible probe does not establish the owner's exact startup filters. Initial reads and three alternating sequential/concurrent rounds use retained read-only connections, a two-second lock timeout, 60-second query deadlines and a 15-minute campaign limit. Preserve partial evidence; no timing-driven reruns.

Matched owner launch `c63ea837-8590-4bfe-9154-a391a6d23ec0` (18:52:35 +02) selected incremental catch-up under All users: compatible snapshot, supported fingerprints, increased count/timestamp comparisons, no forced refresh. No full repair occurred, so the historical 94.842s repair remains unattributed. Splash dismissal was 24.633s, readiness 37.159s and catch-up completion 99.306s. Maintenance counting took 14.910s, collection counting 11.212s, and gallery/global counts each reported 12.983s; these overlap and include application-call waiting/transport. All images appeared, owner-confirmed; responsiveness is unconfirmed. No WebView failure was recorded. This is not speed or reliability acceptance.

The probe records fixed labels, provenance hashes, effective settings, exact-result checks and sanitized query-plan classifications in bounded create-new JSONL. Raw values, identifiers, SQL, paths and errors are excluded. File metadata and SQLite change observations invalidate changed-catalog comparisons. Direct bundled-SQLite latency is not plugin IPC, OS-cold startup, or a subtraction-based estimate of lock waiting. Deliver a measured narrow next proposal or explicit uncertainty; catch-up optimization and black-window diagnosis remain separate.

Safety findings during generated-data verification: an idle SQLite interrupt can be lost before prepare, so the watchdog now keeps interrupting until completion; a deterministic regression covers cancellation-before-prepare. A closed WAL database without sidecars caused read-only SQLite to create sidecars. A header-only preflight therefore rejects that layout before opening SQLite, without creating files, changing permissions, using immutable mode or silently rebasing the comparison. Existing SHM lock bookkeeping is not treated as a catalog-content modification. This is a supported-layout limitation, not permission to perform writes.

Final verification: nine focused tests passed/two opt-in ignores; 968 native library and 18 utility tests passed/27 opt-in ignores; 3,495 frontend tests passed/one intentional skip. Typecheck, lint, Rust formatting and whitespace checks passed. Independent safety/concurrency/privacy and specification reviews are clean; the final teardown check also requires unchanged catalog stamps after connections close. Existing native dead-code, jsdom navigation and unrelated line-ending notices remain. Tracked shipping-file aggregate SHA-256 matched its pre-change baseline (`E90B4AB3732F77CB25DF02331F923A07195AAA291890578DCE38CBB15223B325`); dependencies, bindings and application behavior were not changed by this package. Evidence: `src-tauri/target/count-probe-focused.log`, `count-probe-native.log`, and `count-probe-frontend.json`. New builds, binding generation and release packaging were not needed or run for this test-only change.

One-shot outcome (2026-09-05): after the owner confirmed closure and process preflight found no Ambit process, the authorized invocation stopped in 0.25s with `unsupported-read-only-layout`. Both WAL and SHM sidecars were absent. SQLite was not opened and no count query ran; this is a probe compatibility limitation, not a database failure or evidence about startup latency. The create-new report `src-tauri/target/count-read-only-04b6c1d8-2a18-4707-84eb-6a43837b2694.jsonl` contains ordered provenance and an explicit failed terminal record, with no timing rows. No rerun, library mutation, copy, app launch or policy change occurred.

Decision: inconclusive; actual read-only query measurements remain missing. Return to a separately approved probe-compatibility plan that explicitly resolves SQLite sidecar handling for closed WAL databases before another attempt. Do not weaken the guard, select a query optimization, reopen failed experimental gates or request another owner startup batch. The owner can resume normal app use; no further closed-app wait is needed in this phase.

### Previous package: explain the expensive changed-source repair

Approved 2026-09-05: evidence only, before another optimization. Sixteen retained launches from 16:33 to 17:26 all recorded readiness and no WebView failure; thirteen consecutive launches were 8.043–9.184s, with scope conditions not established. Launch `945cc0f9-4b3c-4a58-9539-66c643b45823` at 17:19:28 reached readiness at 124.846s, including source repair 94.842s and maintenance counting 17.898s; catch-up finished at 131.635s. The owner recalls about eight generated InvokeAI images beforehand, not a controlled four-image test. Native snapshots at 15/30/60/120s each received recent heartbeats, not proof of painted pixels. Fifteen launches retained settings-drain/flush completion and native exit observations; one ready launch has no close timeline, which is unknown rather than failed close. The earlier browser-process exit remains confirmed and unattributed. Stop repeated reliability reproduction batches.

Record the initial admission decision and fixed comparison classifications without changing its boolean policy. Current source already permits qualifying All-users incremental catch-up, but the exact dirty source used by the older executable is not established. The first pre-readiness repair gets one invocation-local aggregate collector: setup, identity reads/paths/matching, legacy-path verification, inventory build/reconciliation, detail reads/paths/extraction, fact/reference writes and batch yields. Every metric retains calls/total/max; filesystem enumeration is nested inside path resolution and excluded from exclusive elapsed accounting. Residual elapsed is unattributed, not SQLite time. Native command/plugin timings include IPC, waiting and decoding. Reference sources replaced are not proof of changed reference contents.

Decision, five coarse stage transitions and one terminal repair report share the existing typed command with additive generated payloads. Separate bounded repair storage (8KiB taken from the ordinary area) preserves the 32KiB terminal reserve, 256KiB file cap and 20-launch retention. First decision/stage/report delivery is deduplicated; diagnostic failures never authorize retries or alter repair outcomes. Reports validate fixed fields and numeric bounds; missing evidence remains unknown. Renderer-stall warning thresholds are excluded from slowest computational-work ranking. No query, index, schema, connection, scheduling, graphics, shutdown or owner-gate changes.

Verification complete: pre-change focused baseline 134 passed/one intentional skip; final frontend regression 3,495 passed/one intentional skip; final native regression 960 library and 18 utility tests passed/27 opt-in ignores. Generated-data parity covers ordered business calls/arguments/results, rowid and compatibility batches, owner isolation, aliases and failure/cancellation with enabled/unavailable diagnostics. Typecheck, lint, generated binding compatibility/drift, Rust formatting, whitespace, guarded frontend build and serial final-source release-profile no-bundle desktop build passed. Independent specification and standards/privacy/concurrency closure reviews are clean. Review ledger: outer diagnostic payload now rejects unknown fields (rather than silently dropping them); a misplaced nullable-fingerprint test fixture was corrected and the full native suite rerun. Existing native dead-code, build plugin-timing, jsdom navigation and unrelated line-ending notices remain. No app was launched. Packaging, full coverage/release verification and real repair attribution are unrun.

Evidence is retained in `src-tauri/target/startup-repair-frontend.json`, `startup-repair-native.log` and `startup-repair-source-provenance.json`. The final source hashes were independently read back and matched; Cargo manifest/lock and path-resolution implementation hashes stayed unchanged. These build artifacts are local evidence, not release assets; the historical journal does not establish the exact source of its executable. Current conclusion is **inconclusive about the dominant repair operation until a new report is captured**, not a performance or reliability fix.

Owner run is next: at most one regular-dev All-users launch after eight new Odin images, with automatic readiness/catch-up evidence and owner image-arrival/responsiveness feedback. Logs remain at `C:/Users/Artemis/AppData/Local/com.ambit.dev/logs/startup/`. Inspect logs before any restart; if blank, leave approximately 60s and stop. If full repair does not occur, do not force it or repeat the batch. Deliver a measured next proposal or explicit inconclusive result, not performance/reliability acceptance. No automatic launch or real-library operation occurred during implementation.

Pre-edit provenance, HEAD `8e4bed6d222c517a4416ddfcf6ce2174e937bd6a` (dirty worktree preserved). SHA-256:

| Source | Hash |
| --- | --- |
| `src/contexts/SyncContext.tsx` | `38B137653EBBA3A97DE9202101350B5F40E3B1D735AA3FF3581C79B72BFC1F53` |
| `src/services/invoke/sourceReconciliation.ts` | `A0C23B06B20A51FBFEE51866BF57F2729FE3A2647F308DE23D95DE76C6D85EFD` |
| `src/services/invoke/ownerScope.ts` | `8ABCAA613A7A91EFA54B99B6D430669404BEFE46A9A8F58950FECF7CC06CE059` |
| `src/services/invoke/pathResolver.ts` | `35FCFA6969CE51157497CA44E78A2546C9E1732DBC3CE576AD8809DAC0FBAF5E` |
| `src/utils/startupDiagnostics.ts` | `387CE2DED0BC3DB9247324E43E6444798D765AE136FF5894C3440CE6ED717766` |
| `src-tauri/src/startup.rs` | `DB632E46C7E16D1C87E3AB3D8F591DFBCE5D21F9DF0B7884ABBDC077ECDF1743` |
| `src-tauri/src/startup_log.rs` | `5472F15437C516F522095D143E4EE2C127BCDBDB3F097F680EECFC906EDF54D5` |

### Previous package: WebView exit details and close/relaunch timeline

Approved 2026-09-05: extend diagnostics only. Six unchanged launches all reached readiness (23.525, 7.917, 7.884, 7.908, 7.919, 7.953 seconds). A later owner-reported sequence was launch, change owner scope twice, close, then relaunch into a black window. Launch `25b48ac6-9f32-4d4e-ac58-913c9b404477` completed owner preparation at 6.319s and privacy at 6.354s, then reported `browser-process-exited` at 7.043s. At 15.001s heartbeat age was 10.121s; readiness and normal shutdown were unrecorded. No later snapshot establishes how long it remained open. The preceding launch recorded readiness and handler removal; neither an incomplete settings flush nor overlapping browser processes is established. Two subsequent launches reached readiness at 9.081s and 8.195s without recorded WebView failures. Owner switching is a reproduction condition, not an attributed cause.

Capture the first numeric WebView exit code per fixed failure classification, with null for unavailable details, alongside the existing callback timestamp. No descriptions, module paths, frame data or dumps. Microsoft documents that the reason for browser-process-exited is always Unexpected, so a separate reason field would not distinguish this failure: [ProcessFailedEventArgs2](https://learn.microsoft.com/en-us/microsoft-edge/webview2/reference/win32/icorewebview2processfailedeventargs2). Use the existing COM extension and a Windows-only direct dependency on already-resolved windows-core 0.61.2; no version upgrades.

Add first-occurrence, fixed-label close stages for settings drain/flush and exit invocation/failure, plus native close request, exit request, window destruction and exit. Frontend transport is best effort, never awaited, never replayed, and bounded to nine stage sends; native receipt validates the active launch ID. Native observations use atomics and the existing off-callback journal writer/reserved capacity. This is not a complete retry-attempt trace: late IPC may be lost on exit and absence is unknown, not a failed flush. Callback/receipt timestamps define the timeline, not JSONL line order. Keep the existing close admission, drain, flush, native optimization and exit behavior unchanged.

Verification targets: nonblocking callbacks while journal storage is locked, duplicate/unknown/foreign-launch rejection, unavailable/signed exit codes, saturation and terminal reserve, pending/rejected/throwing frontend transport, close failure/retry behavior, generated command compatibility, native/frontend regressions and independent lifecycle/privacy review. No app launches, real-library access/copies, scope or credential changes, automatic recovery, graphics changes or database tuning.

Review remediation: the pre-existing synchronous final journal flush in Tauri's Exit callback could wait on diagnostic storage. Exit now only signals/unparks the existing observer; that worker owns finalization. No join, exit delay or process-lifetime extension is added. The final summary/native-exit record can therefore be absent when the process terminates first, even after a normal close. Treat that tail as unknown, not a failed close. A lock-held regression verifies nonblocking exit signaling. The separate specification and concurrency/privacy reviews inspect this correction as well as the additive fields.

Final verification: 3,479 frontend tests passed with one intentional skip (`src-tauri/target/startup-exit-frontend.json`); 954 native library and 18 utility tests passed, with 27 opt-in ignores. Lint, typecheck, generated binding compatibility/drift, Rust formatting, whitespace, guarded frontend output and the serial final-source release-profile no-bundle desktop build passed. Independent specification and standards/concurrency/privacy closure reviews are clean. Subscription failure containment was also added during self-review. Only the root direct reference to existing windows-core 0.61.2 is new dependency bookkeeping; earlier dirty dependency/binding changes were preserved. Existing dead-code, build-timing, jsdom navigation and unrelated line-ending notices remain. No app was launched. Packaging, the full coverage/release gate and real failure-code capture with this version are unrun. This verifies the diagnostic machinery, not the blank-window cause or a reliability fix.

After verification, owner reproduction is bounded to ordinary close/relaunch versus the same two owner changes followed by close/relaunch, regular dev only. At most three cycles per condition; stop at the first black window, leave it about 60 seconds, and inspect automatic logs before further restarts. No images or stopwatch needed. A clean batch means not reproduced. Numeric exit codes narrow investigation but may still be inconclusive; do not claim a root-cause fix or resume speed acceptance.

### Previous package: explain intermittent blank-window startups

Approved 2026-09-05: evidence only, not a speculative rendering/database fix. The nine owner launches recorded readiness of 25.560, 8.854, 8.445, not recorded, 9.018, not recorded, 25.629, 10.310 and not recorded seconds, in creation order. The incomplete fourth/sixth/ninth journals reached React mount and stopped around 4–6 seconds; observation duration before closure is unknown. They are neither proven crashes nor proof that the last query caused the blank window. Warm/changed-source labels are not inferred from these timings. Reliability acceptance has not passed.

Extend the existing bounded local journal with a separate two-second, single-outstanding-request renderer heartbeat and native snapshots at 15/30/60/120 seconds. Missing or >10-second-old receipts mean renderer/transport responsiveness unknown, not a crash. Keep actual observer delay, last completed renderer phase and bounded outstanding phase counts. Native Windows ProcessFailed observations retain fixed process-failure classifications after readiness without changing historical readiness. Registration/teardown failure is explicit; callbacks must not wait on disk or database work. Journal retention remains 20 launches/256KiB with terminal reserve, no raw values or dumps.

Preserve the 15-second premount warning and add a separate 30-second postmount warning only inside an existing loader. Never recreate/dismiss the loader, cover owner recovery or change privacy gates. Page warnings and heartbeats cannot prove painted pixels or repair a dead WebView. Additive diagnostic command bindings are generated; no library/schema, query/index, connection policy, graphics, lifecycle recovery or startup scheduling changes.

Verification: final frontend suite 3,471 passed/one intentional skip; native suite 951 library and 18 utility tests passed/27 opt-in ignores. Lint, typecheck, generated binding drift, Rust formatting, whitespace, guarded frontend and serial final release-profile no-bundle desktop builds pass. Final frontend evidence is `src-tauri/target/startup-reliability-frontend-final.json`. Existing compiler dead-code/build timing notices and jsdom navigation notice remain. The only new Cargo resolution entry is the root dependency on existing Windows `webview2-com` 0.38.2; no versions changed. The heartbeat command is additive and generated bindings are current. No application was launched, real library accessed/copied or production/credential settings changed. Packaging, full coverage/release verification and real WebView failure reproduction are unrun.

Independent specification and standards/concurrency/privacy closures are clean. Review ledger: wrong-thread teardown now reports unavailable and preserves the owner thread's cleanup opportunity; native page-load callbacks defer disk writes; warning callbacks explicitly reject late execution after fatal presentation. Regression tests cover those boundaries, single-flight heartbeat stop/rejection/hang/deadline behavior, missing/recovered receipts and reserved evidence under saturation. Simulated tests establish diagnostic behavior, not the real blank-window cause.

Owner reproduction is next: regular dev, unchanged library and InvokeAI scope, up to six unchanged launches stopping at the first blank window. Leave that window open approximately 60 seconds and report appearance/responsiveness; read automatic journals at `C:/Users/Artemis/AppData/Local/com.ambit.dev/logs/startup/` before more restarts. No new image generation or stopwatch timing is required. No reproduction in that bounded batch means not reproduced, not fixed. Current conclusion remains inconclusive about the cause; return a supported classification and separately proposed next step after this batch. Do not resume speed acceptance yet.

### Previous package: single authoritative startup collection load

Approved 2026-09-05: remove redundant pre-owner collection counting, preserving fully prepared opening and existing SQL, indexes, plugin, connection settings and owner/privacy semantics. The previous warm journal contains overlapping 10.560s and 8.465s collection calls; their durations are not additive savings.

Collection preparation now shares lightweight schema/legacy-store work without counting. Existing legacy import and marker failures remain nonfatal and retain retry data; cleanup requires a successful membership read before deleting an empty legacy mock collection. Synchronization startup owns the final counted load: configured Invoke uses its authoritative owner-cache refresh; no-Invoke and trusted-offline starts load once. First unselected startup keeps counted readiness false and can show selection/error controls and Settings without exposing the gallery. Hard standalone failures use the existing bootstrap fallback rather than declaring an empty library loaded.

Generation invalidation rejects obsolete counted publication and cancels obsolete thumbnail/smart work. Initial hydration has a pending-generation permit and is completed only by the current generation; authoritative full/dirty work satisfies it, while restored/selective startup retains its initial hydration. Later runtime switches do not replay completed startup hydration. Normal post-write, privacy and owner-transition refreshes remain intact; exactly one counted pass is the ordinary stable-start invariant, not a promise across legitimate mutations.

Verification: the final focused provider/store/repository/App set passes 284 tests with one intentional skip, including Strict Mode, initial owner modes, first selection, trusted offline, retry/failure, supersession and hydration. The final-source full frontend suite passes 3,456 tests with one intentional skip. Native regressions, typecheck, lint, binding drift, Rust formatting, whitespace, guarded frontend build and the serial release-profile no-bundle desktop build pass. Existing native dead-code/build-timing notices and the test environment's navigation notice remain. Final JSON reports are retained under `src-tauri/target/startup-scheduling-{focused-final,frontend-final}.json`. No application was launched, no real library was accessed or copied, and no production/credential change occurred. Packaging and the complete coverage/release gate are unrun.

Review ledger: nonfatal legacy import/marker handling restored; first unselected admission no longer triggers initial counts; canceled hydration cannot consume the winning generation's permit. Each has focused regression coverage. Independent specification and standards closures are clean. Earlier dirty changes and rejected experimental evidence remain preserved and uncommitted.

Owner acceptance uses regular dev only: first-after-change separately, five unchanged launches and three launches after four new Odin images under All users. Existing automatic journals at `C:/Users/Artemis/AppData/Local/com.ambit.dev/logs/startup/` replace the stopwatch. Report readiness, count occurrences and separate catch-up completion; missing/truncated evidence is incomplete. Owner reports visual failures, responsiveness and image arrival. Latest real readiness remains 36.797/20.893/34.720s and changed catch-up 53.510s; targets remain <10s warm and <30s changed-source. This implementation is not yet a measured startup-speed improvement or a black-screen attribution.

### Previous package: minimal connection policy — stopped at the failed screen

Approved 2026-09-05: identify and screen only, with a 128 MiB additional median peak working-set/private-commit allowance. All fixtures are generated; the upstream plugin, shipping connection policy, queries, indexes, migrations and startup behavior remain unchanged. At most one candidate may advance; no timing-driven reruns or fallback candidate after a failed screen.

The fixed attribution campaign completed all 12 contrasts, six counterbalanced pairs each, retaining 864 exact-result maintenance reads on the unindexed wide 10k/1k fixture. Defaults versus the legacy bundle was 131.402/13.698ms warm; A/A was 127.880/127.646ms and passed the original allowance. **Mmap was the only individually effective setting**: the existing 256 MiB mapping alone changed 123.987 to 12.175ms, improving all six paired warm medians. Removing mmap from the legacy bundle changed 12.715 to 134.101ms. Cache, temporary storage, synchronous mode and busy timeout did not independently meet the advancement threshold. This isolates the large effect on this generated fixture, not every real startup delay or every historical IPC sample.

Only the approved mmap ladder advanced. Each arm ran in a fresh worker process; fixture generation/copying stayed in the controller. Both unchanged queries used known retained physical connections, with acquisition separate from execution/result decoding. The 432 query samples matched the shared reference.

| Mmap limit | Warm maintenance baseline / treatment | Improving pairs | Selection |
| --- | ---: | ---: | --- |
| 16 MiB | 130.480 / 125.687ms | 6/6 | Below 20% improvement |
| 32 MiB | 137.132 / 119.713ms | 6/6 | Below 20% improvement |
| 64 MiB | 137.440 / 85.919ms | 6/6 | Selected; only eligible candidate |

The 64 MiB treatment's small-worker median peak working set was 93.76 MiB versus 27.61 MiB baseline (+66.16 MiB); median peak private commit was 10.09 versus 11.31 MiB. Treatment maxima were 94.41/10.75 MiB respectively. For 16/32 MiB, treatment maxima were 45.13/11.83 and 58.68/11.75 MiB. These are fresh process-wide measurements with two held connections, not ten-connection or app memory guarantees. Every individual measurement and median is retained; mapping limits are not allocated-RAM estimates.

**The selected policy failed the first upstream-IPC cell (10k, sequential)** after all six pairs and 144 exact-result query samples. First maintenance increased from 72.295 to 117.908ms (+45.613ms), exceeding the approved +20ms allowance. Warm maintenance was 123.412/113.483ms. First/warm collections improved from 14.318/15.263 to 9.027/8.786ms, but do not override the failed gate. Both arms first ran the unchanged frontend setup; each pair had matching initial settings distributions (one legacy-tuned connection and nine defaults). Candidate mmap was then set to 64 MiB on all ten. Generated TEMP markers and post-query snapshots verified that the same ten physical connections survived; no replacement or retuning occurred. Actual per-query connection assignment was not recorded. This is a failed policy screen, not proof of a universal penalty or an index-caused regression.

Decision: **stop this policy approach; do not integrate or expand the mapping limit**. The concurrent-small, all 150k, selected-owner/non-Invoke correctness smoke and separate ten-connection memory branches remain unrun because the earlier gate failed. No alternative candidate was selected. Return to planning before further optimization; the original failed index qualification remains unresolved. No owner retesting is requested. Latest real readiness remains 36.797/20.893/34.720s, with changed-source catch-up separately at 53.510s; targets remain under 10s warm and under 30s changed-source readiness.

Evidence: `src-tauri/target/connection-policy-{attribution,small,screen}.jsonl` and corresponding `.log` files. The three campaigns ran once, with recorded spans 90.958/40.217/20.982s including generated preparation (152.157s total, well within 60 minutes; compilation excluded). Feature-owned evidence uses create-new protection. Strict validators reject incomplete/malformed/extra-field/mispaired records, altered settings, absent memory and counterfeit aggregates; they distinguish a completed failed gate from an incomplete passing prefix. Parser/selection review corrections did not change or repeat measured workloads.

Verification: 942 native library and 18 utility tests passed, with 27 opt-in ignores; all three retained validators and active evidence tests passed. The unchanged baseline initially hit the existing thumbnail root-probe timing test, which passed alone and on baseline confirmation; the final full suite passed. Independent concurrency/isolation and evidence reviews are clean. Rust formatting and whitespace checks pass; existing dead-code/CRLF notices remain. Recorded tracked-file hashes, Cargo manifests/lock and bindings were unchanged before these documentation edits; original failed-index evidence is preserved. Generated catalog directories self-cleaned. No frontend/build/release checks or real-library/app operations ran in this test-only phase; no shipping API, type, schema, plugin or connection-policy change and no commit.

### Previous package: diagnose the failed maintenance benchmark

Approved 2026-09-05: diagnosis only. Preserve the failed qualification and the original index-only candidate. Two fixed generated 10k/1k reproduction blocks, each six A/B and six A/A pairs using unchanged measurement boundaries, precede one controlled probe campaign. All samples must match a shared unindexed reference. Retain individual samples, pair medians and pooled medians; a passing rerun cannot qualify the index. No shipping changes or owner launches. Measurement budget is 30 minutes excluding compilation; stop rather than expand the investigation.

Reproduction completed in two separate processes (61.57s and 36.67s including generated preparation). The original large positive pooled A/B difference did not recur consistently: concurrent warm maintenance was 19.531/64.612ms in block 0 and 79.950/32.756ms in block 1. A/A itself exceeded the original allowance in block 0 (77.780/109.829ms). Median within-pair warm differences were much smaller: A/B +3.209/+0.910ms and A/A +3.720/+10.545ms. These are descriptive pair summaries, not replacement acceptance thresholds. This reproduces timing instability without establishing an index-caused regression or clearing the original failure. Both captured logs pass validation of all 576 samples per block, ordered treatment headers, exact series positions and completion markers. Original logs were not rewritten or rerun after validation corrections.

Predictions fixed before the single probe campaign:
- Transport: if callback delivery/caller resumption accounts for the long timings, dispatched-to-callback or callback-to-caller measurements should localize it; lightweight concurrent IPC is a control. This does not by itself separate plugin waiting from database work.
- Pool lifecycle/assignment: if automatic checkout or connection assignment is necessary for the pattern, retaining explicit physical SQLx connections across reads should remove it. Direct SQLx totals are a different boundary, not substitutes for IPC acceptance.
- Settings: if heterogeneous settings drive the pattern, swapping the legacy-tuned connection between maintenance and collection should move the effect predictably. Tune only generated held connections; retain default/default controls.
- Access path: if the index independently slows maintenance access, identical-settings maintenance-only measurements and query plans should exhibit a corresponding change. A changed plan alone is not a measured cause.

The single eight-case probe campaign completed in 85.18s including preparation (77.74s measured runner span), retaining all 936 query samples from six counterbalanced pairs per case, first plus five warm reads. It verified exact results, WAL shared by both connections, and every physical connection's local settings. All three campaign executions together took 183.42s including generated preparation, within the 30-minute measurement budget; compilation is excluded. No second probe campaign ran.

Warm maintenance medians (ms; held SQLx execution/result decoding, not IPC or pure SQLite execution):

| Controlled condition | Unindexed | Indexed |
| --- | ---: | ---: |
| Maintenance alone, defaults | 119.30 | 123.28 |
| Maintenance alone, legacy settings | 12.51 | 10.57 |
| Concurrent, both default | 125.42 | 122.17 |
| Concurrent, maintenance legacy / collection default | 13.08 | 11.55 |
| Concurrent, maintenance default / collection legacy | 122.32 | 130.13 |

The settings **bundle** causes a reproducible fast/slow difference on the generated fixture, even with physical connections retained (so repeated acquisition is not necessary). All 180 default-maintenance warm samples in these held conditions exceeded 90ms; none of the 120 legacy-maintenance samples did. This is descriptive separation, not a new gate. Maintenance query plans are identical with/without the index. Lightweight IPC warm medians are about 0.20ms. Maintenance concurrent IPC callback-to-caller maxima are below 0.06ms; across both query types/all IPC warm samples the maximum is 0.8193ms. Thus post-callback caller delay does not explain the roughly 100ms split. Time before the callback still combines database, plugin and scheduling work.

Classification: **demonstrated connection-settings sensitivity; original index regression remains unresolved**. IPC did not record physical connection assignment, so the exact explanation for each historical sample remains an inference. The experiment does not isolate cache size, mmap or another individual setting, measure memory, establish OS-cold behavior, clear the failed index gate, or prove an actual startup speedup. Do not restore the previously rejected broad per-connection patch. Recommend a separately approved, narrow and memory-bounded connection-settings investigation before reconsidering index qualification; retain the original acceptance thresholds.

Evidence under `src-tauri/target`: `count-diag-reproduction-0.log`, `count-diag-reproduction-1.log`, `count-diag-probes.log`, `count-diag-retained-final.log`, and `count-diag-native-final.log`. These are local build artifacts, not release assets. The original failed `count-index-secondary.log` SHA-256 remains `BB0666DB96E7E7DFA12583761A20C4B385BE5F445D9515A1E2E86F86AF4D9B1C`.

Verification: 939 native library and 18 utility tests passed; 19 opt-in tests remain ignored in the ordinary suite. Both read-only evidence validators passed, including complete probe scheduling, settings and sample validation. Active regressions cover missing/duplicate/mispaired/interrupted data, invalid timings and altered/swapped recorded settings. Independent standards/concurrency and specification/evidence reviews are clean after scoped corrections to envelope validation, probe checkpoint parity and recorded settings checks. No measured campaign was repeated. Rust formatting and whitespace checks pass; existing compiler dead-code and unrelated CRLF notices remain. Recent generated fixture directories were absent after cleanup. No public APIs, bindings, frontend or shipping database code changed this phase; frontend suites/typecheck/lint/builds, desktop builds, memory/write qualification and owner acceptance were not run. Prior mixed uncommitted changes are preserved. No index integration, new qualification, plugin/pool-policy/query/scheduling changes, real-library access, credential change or app launch.

### Previous package: collection-count index qualification

Approved 2026-09-05: test the narrow collection-count access path, integrating only after exact correctness and performance gates. The four-configuration bundled SQLite 3.46.0 experiment passed with equal results: baseline and narrow-view-only plans contain image-table Column operations; index-only and combined plans both use the covering index and contain none. This is compiled-plan evidence, not runtime opcode hit counts, bytes read, or startup timing. The fixture has 146,182 wide generated rows and skewed memberships; that initial access fixture still has short IDs.

The owner subsequently approved qualifying the simpler **index-only** candidate. The proposed narrow view is therefore test-only historical control, not a shipping addition. Keep the existing count SQL and canonical owner view unchanged. Candidate index: `images(id, invoke_source_id, invoke_owner_id, invoke_scope_hidden)`, with index-specific ANALYZE. No migration is registered before qualification.

The actual upstream IPC primary gate passed all 16 comparisons, using a 2,700,845,056-byte generated catalog, 80-character IDs, wide metadata and mixed owners. Six counterbalanced pairs per scope each contain one first read and five warm reads per query/boundary. Complete rows match one shared unindexed reference. Concurrent warm collection medians: All users 2,137.162 ms baseline / 442.558 ms index; selected owner 2,178.119 / 297.223 ms. Warm sequential collections: 1,616.652 / 201.633 ms and 1,545.001 / 187.527 ms respectively. These passed the 20% primary improvement and 10%/+20 ms non-regression rules. First reads are not OS-cold, IPC totals are not pure query execution, and there is no app-readiness claim.

Secondary qualification **failed** the first 10k-image All users cell after all six pairs. The small fixture has 1,000 memberships, with 120 in its largest collection and 377 populated collections; the large fixture's 15,858-member skew is unchanged. Seven comparisons passed, but concurrent warm maintenance increased from 76.985 ms to 113.276 ms (+36.291 ms), exceeding the approved +20 ms allowance. Small concurrent warm collections improved from 8.890 to 3.987 ms. This is a failed predefined gate, not proof that the index universally slows maintenance. Do not retry until a passing sample appears or reinterpret the threshold after measurement.

No migration or shipping SQL change is integrated. Small selected-owner/non-Invoke, large non-Invoke, overlapping-two-count, write, separate-process memory, index-build/disk-overhead, migration and owner-acceptance gates are explicitly **unrun**. The narrow view remains a test-only control. No memory/write/startup claim follows from the collection improvement. The next decision should isolate the small concurrent-maintenance effect and pooled-connection variability before revising qualification or integrating anything; do not combine maintenance-query or scheduling changes speculatively.

Raw evidence: `src-tauri/target/narrow-count-access-gate.log`, `count-index-primary.log`, and `count-index-secondary.log`. Primary completed in 1,353.75 seconds including preparation/copies/cleanup; secondary stopped in 33.66 seconds. These durations are test-run totals, not startup timings. The secondary log retains its historical generic helper message saying 'primary qualification failed'; source wording now says 'timing qualification failed', with no gate/workload change or performance rerun. Independent reviews verified complete samples, exact-result assertions, the failed gate and absence of a shipping candidate. Generated-only fixture confinement and existing startup/owner/privacy behavior remain intact. No automatic app launch, import, real-library copy/replacement or production/credential mutation. Owner retesting stays paused; startup targets remain under 10 seconds warm and under 30 seconds changed-source readiness.

Verification for this package: final frontend suite 3,436 passed/one intentional skip; native suite 935 library plus 18 utility tests passed/15 opt-in ignores. Typecheck, lint, binding drift, Rust formatting and whitespace checks passed. An earlier concurrent verification run timed out in an unrelated donation-dialog test; it passed alone and in the final full frontend run. Independent standards/specification and evidence/documentation closure reviews are clean. No recent generated count-catalog remnants remained. Frontend/desktop builds, packaging and owner launches were not run because no runtime integration qualified. Existing compiler dead-code and working-tree line-ending warnings remain; prior mixed changes are preserved and uncommitted. No benchmark was rerun to seek a passing result after the failed gate.

### Previous package: upstream restoration and count attribution

Status: upstream restoration and bounded investigation complete and verified (2026-09-05).

The exact pinned registry SQL plugin 2.4.0 replaces the rejected local patch. Cargo's vendor override is removed without dependency upgrades. The 31 vendor files, including provenance and licenses, are recoverably archived under `src-tauri/target/sql-plugin-rollback-20260905/tauri-plugin-sql`. Earlier startup diagnostics, failure presentation, owner synchronization and shared initialization fixes remain. The legacy frontend PRAGMAs and six index operations remain unchanged. The actual MockRuntime harness is retained; its lock-release acceptance assertion is explicitly opt-in because upstream still retains the registry guard during operations.

Both plugin experiments are withdrawn. The final lock-only gate failed three of 48 comparisons: +10.8% All users warm concurrent collections, +10.8% selected-owner warm concurrent collections and +12.1% non-Invoke first concurrent maintenance. Earlier matrices varied, so this is a failed acceptance gate rather than causal proof of a particular regression. Per-connection tuning also regressed latency and substantially increased peak memory. Original evidence remains under the existing `src-tauri/target/sql-plugin-ab-evidence-*` directories.

Latest owner launches remain 36.797 seconds first-after-change, 20.893 seconds warm and 34.720 seconds after four new Odin images. Catch-up completed at 34.903 / 18.971 / 53.510 seconds respectively; readiness is a separate boundary. Images appeared and browsing was responsive. No new app launch was performed for this package.

#### Attribution evidence

- Initial collection count / source-open completion nearly coincide on first-after-change (22.433 / 22.418 seconds) and changed-source (21.703 / 21.702 seconds). This is consistent with known upstream registry contention; it does not measure pure query execution.
- Warm launch has overlapping collection counts: 10.560 seconds from native receipt about 4.488 seconds, and 8.465 seconds from about 6.579 seconds. Maintenance spans about 6.535-18.690 seconds (12.155 seconds). Initial store loading and authoritative owner-cache refresh use separate entry points, so initialization deduplication does not merge them.
- The owner-cache refresh awaits collections and maintenance together. Maintenance determines its completion in all three retained launches (5.407 / 12.155 / 5.053 seconds). Routine background metadata maintenance is distinct from these sidebar maintenance counts.
- Bundled SQLite 3.46.0 read-only probe, with Ambit absent: collection first read 27.973 seconds, maintenance first read 5.094 seconds; warm sequential medians 1.559 / 1.630 seconds and concurrent medians 1.739 / 2.030 seconds. All results matched. These use default read-only connection cache/mmap settings and ordinary WAL visibility, without plugin IPC, persistent tuning or OS-cold control. No subtraction attributes the difference to a specific waiting layer.
- Regular dev has 146,182 images, 135,468 source-associated images, 131,741 unique-image memberships across 377 nonempty collections (379 total); largest collection 15,858 members. Database size is 2,898,239,488 bytes, with 57 free pages of 4,096 bytes. The images table occupies 2,214,772,736 bytes.
- Aggregate non-null mean text lengths (characters): original metadata 5,339.65; current metadata and original parsed metadata each 3,942.98; positive prompt 517.38. Old generated fixtures populate only one uniform 2 KiB metadata payload and distribute memberships evenly. They therefore underrepresent real row width and skew. Aggregates expose no contents, paths or owner identities.
- Collection query plan traverses indexed memberships then image primary-key table lookups and a grouping temporary tree. Maintenance scans scoped image rows. Expensive real standalone reads establish that plugin contention is not the sole cause; storage/cache and wide-row access remain plausible contributors rather than isolated causes.

#### Verification and handoff

The aggregate-shaped upstream IPC probe passed with exact reference results on every sample. Three-round sequential medians were 2,669.867 ms collection / 2,424.058 ms maintenance; concurrent medians were 2,477.210 / 2,741.529 ms. Generated database size was 2,541,879,296 bytes; peak whole-test-process working set was 350,162,944 bytes (333.94 MiB), including fixture preparation. All ten physical connections were inspected: one had the legacy frontend cache/mmap/synchronous/temp/busy settings and nine retained SQLx defaults, while all used WAL. This distribution is an observation, not a per-connection guarantee. No pure execution/pool/registry breakdown was measured. Uniform synthetic payloads omit real outliers, identifiers and physical layout; this one All users experiment does not establish performance in other scopes or OS-cold startup.

The first generated attempt rejected malformed synthetic JSON before measurement. A subsequent run completed queries but failed SQLx teardown; its timings are not the accepted evidence. The fixed test enters the runtime for the entire fixture/connection lifetime and completed successfully in 119.30 seconds including generation/cleanup. Only the abandoned generated catalog was removed after validating its exact path and contents. The successful run self-cleaned. The real-catalog timing run preceded addition of the interruption watchdog; query text and workloads were unchanged, and the final read-only write-rejection regression passes.

Verification: 3,436 frontend tests passed (one intentional skip), 932 native library tests passed (12 opt-in ignores), 18 utility tests passed, typecheck/lint/binding drift and Rust formatting passed. Independent standards and specification reviews are clean; the teardown correction received focused closure review. Guarded frontend output and the release-profile no-bundle desktop build passed serially. Existing compile-time dead-code warnings remain; no new warning was introduced by the rollback. Cargo resolves registry 2.4.0 with no unrelated dependency upgrades. Packaging, the complete coverage/release gate and real-app owner acceptance were not run; no app was launched. Earlier mixed working-tree changes remain uncommitted and were not reset.

An opt-in read-only probe rejects writes, limits query execution to 60 seconds, and retains only aggregate evidence. Generated IPC comparison approximates measured payload lengths and membership skew; it does not copy real data or promise identical physical layout. Raw evidence is retained under `src-tauri/target/sql-plugin-rollback-20260905`. These build-artifact directories are local evidence, not durable release assets.

Next recommendation is one bounded count-access-path proposal: evaluate ways for the unchanged count semantics to avoid fetching wide image rows, using the aggregate-shaped catalog and exact scoped-result gates. Duplicate initialization/owner-refresh work remains a separate identified scheduling concern; it is not automatically folded into that proposal. No query rewrite, new index, cache, pool-policy or scheduling change is included here. Owner retesting stays paused until a measured candidate is available. Targets remain <10 seconds warm and <30 seconds changed-source readiness, with catch-up separate. The earlier black-screen cause remains unproven.

### Previous package: reliable startup evidence and initialization

Approved baseline: 30.879 seconds from native boot to splash dismissal start and 32.731 seconds to readiness after four new Odin images under All users. Native owner visibility is now 382 ms; collection counting and subsequent maintenance-count refresh remain slow. The reported black-screen run has no recoverable complete trace and is not attributed yet.

- Retain a dedicated local JSONL per native launch, latest 20 files, 256 KiB each with terminal-record capacity. Fixed classifications/timings/counts/random launch IDs only; never raw errors, paths, prompts or owner identities. Diagnostic failure must not stop startup. Record native boot/page milestones independently, correlate renderer entry/mount, splash, safe first page and readiness, and record initial Invoke catch-up separately. Print a readiness summary; missing milestones remain not recorded.
- Run an independent pre-module bootstrap, visible static loader and outer provider error boundary. Script/runtime failures show a plain-language fallback and available launch identifier. A 15-second missing React mount changes the status to a stall message, not success or failure. No timer dismisses the loader and no automatic restart occurs. A WebView that never loads or stops executing cannot be repaired by a page fallback.
- Coalesce the entire main database initialization, including existing optional PRAGMAs/indexes. Opening failures allow retry; optional setup failures report but preserve an otherwise usable database. Share Invoke source opening promises by resolved identity, evict failed opens and invalidate on explicit retry/source replacement without closing in-flight connections or replaying operations.
- Exercise the installed SQL plugin through Tauri MockRuntime IPC using generated databases. Reproduce registry contention with controlled synchronization, inspect connection-local PRAGMAs on simultaneously held pool connections, and measure unchanged collection and maintenance counts on the representative generated catalog. Total IPC latency is not pure SQLite execution. Plugin changes, replacement pools, new indexes and query redesign require a subsequent proposal.
- No automatic app launch/import, profile recreation, real-library copies/replacements, production mutation, credential changes, schema/migration changes, new caches, indexes, or progressive counts. Keep all owner/privacy and fully prepared opening gates.

Acceptance: focused failure/concurrency/privacy/retention regressions, frontend/native suites, typecheck/lint/binding checks, serial frontend and desktop compatibility builds, and independent concurrency/privacy review. Regular-dev owner acceptance uses five unchanged starts and three starts after four new Odin images under All users. Automatic logs replace stopwatch measurements; the owner reports visual failures, responsiveness and image arrival. Current targets are under 10 seconds warm and under 30 seconds for ordinary changed-source startup; neither is claimed before measurement. Earlier targets below are historical evidence, not a competing acceptance gate.

### Previous-package evidence

- Verification (2026-09-04): full frontend 3,430 passed/one intentional skip across 283 files, followed by 39 focused concurrency tests after strengthening pending-caller/source-isolation/late-failure assertions. Full native 929 library plus 18 utility tests passed/eight intentional ignores; the representative plugin benchmark was separately run and passed. Typecheck, lint, generated binding generation/drift check, Rust formatting, whitespace checks, guarded production frontend and serial release-profile no-bundle desktop builds passed. Independent review closure is clean. No installer/full coverage gate or real WebView/app launch was performed. Regular-dev visuals, first-page responsiveness, new-image arrival and under-10-second/under-30-second targets remain owner acceptance, not claimed results.
- Next owner run: use regular dev unchanged, then after four new Odin images with All users; continue to five unchanged and three changed-source launches. No stopwatch is needed. Records will be created at `C:/Users/Artemis/AppData/Local/com.ambit.dev/logs/startup/startup-<native launch ID>.jsonl`; readiness emits `[Startup summary]`, with catch-up completion separately recorded. Report visual failures, responsiveness and whether the four images appear. Share these dedicated redacted startup records rather than assuming the general application log is value-redacted.
- Actual installed SQL-plugin MockRuntime tests pass for IPC confinement, controlled registry/pool waiting and multiple simultaneously held connections. Test-only Common Controls v6 linkage is needed for the Windows mock executable; no production builder/profile is used. In the controlled ten-connection test, nine connections retain defaults while one receives the frontend settings. Normal unconstrained initialization followed by three simultaneous acquisitions likewise found one configured/two defaults. Defaults observed: busy timeout 5,000 ms, cache -2,000 KiB, synchronous FULL, temp store default and mmap 0; intended calls are 60,000 ms, -64,000 KiB, NORMAL, MEMORY and 268,435,456 bytes. WAL persists across connections. The runtime settings and pool size are unchanged.
- Representative generated 150k-image/132k-membership/379-collection benchmark: unchanged collection and maintenance SQL returned identical results over six All users and six selected-owner rounds through actual plugin IPC. Warm collection/maintenance IPC medians were 963/467 ms sequential and 658/609 ms concurrent for All users, 875/471 ms sequential and 624/671 ms concurrent for selected owner. Direct SQLite measurements use a separate rusqlite connection; fixed direct-first ordering prevents treating subtraction as registry/pool wait or an optimization comparison. The benchmark did not reproduce the minute-long real startup.
- Evidence: `C:/Users/Artemis/AppData/Local/Temp/ambit-sql-plugin-evidence-1788530841028.jsonl`. All generated fixture directories are absent; three aborted/transient fixtures were recycled, not permanently removed. Neither real library was accessed by these tests. Plugin locking/pool-configuration remedies require a separate approved phase; no further optimization was attempted.
- Review ledger: hidden provider-failure fallback (blocking) corrected by explicit bootstrap fatal presentation plus a CSS-independent outer boundary; late failure during splash fade corrected by retaining/recreating the overlay; retained launch identity corrected for failures after handshake. Build inspection found Vite merging both HTML module entries into the application graph, so a single diagnostic entry now defers the app via dynamic import without awaiting native transport. Its own script-load failure is fatal, while unavailable native diagnostics stay nonfatal. The production guard verifies a 26 KiB static diagnostic dependency closure separate from the 314 KiB deferred app chunk. Independent standards/concurrency, specification/privacy and plugin-test reviews closed clean. These fixes do not establish the cause of the earlier black screen.

### Retained earlier packages

1. Establish latest-main test baseline and bounded startup diagnostics: database phases, owner discovery/cache repair, facets, collections, privacy, first safe page, splash dismissal, and overlapping maintenance. Diagnostics contain fixed phase names, durations, repair actions and launch IDs, never library paths, owner identities or prompts. Use synthetic databases for automated tests; real-world acceptance uses regular dev without copying or replacing either library.
2. Gate automatic thumbnail optimization (30-second delay), metadata checking (3-second delay), and production backup (120-second delay) on actual startup readiness. Retain manual controls and existing priority/cancellation. Readiness derives from existing gates plus a successfully loaded safe first page (including empty results), never from optional-job completion. Statistics demand is transient; cancel further prompt batches on close and reject stale scope/filter results.
3. Replace thumbnail batch `full` dirtiness with existing resource/collection selective invalidation for affected scopes. Preserve set-based writes, unrelated full markers, generation handshakes, rollback, and retry-only non-invalidation. No schema migration.
4. For a current, fingerprinted All users snapshot, admit the saved view and use existing timestamp-based startup catch-up for ordinary source changes. Do not advance its fingerprint before successful catch-up. Selected-owner source drift, missing or incompatible snapshots, path/schema upgrades, unknown source state and forced All users repair retain full reconciliation. Count collection members by joining visible `scoped_collections` to memberships and scoped images through existing indexes. No migration, public type, cache-table or startup sequencing changes; selected-owner incremental optimization is deferred.
5. Reproduce owner-preparation contention through actual native routines before correcting it. Owner visibility and board reconciliation now acquire immediate write transactions before reading, each with a 2.5-second entry lock-wait budget (five seconds combined for those two acquisitions, not the entire startup). Restore the connection timeout afterward; do not replay multi-step workflows or weaken visibility gates. Attribute failed preparation stages in bounded typed diagnostics, and forward actual sync outcomes so blocked or queued work is not reported as successful. Measure collection latency separately; a cache/schema redesign or partially prepared opening requires a separate proposal.
6. Preserve fully prepared opening while removing redundant Windows identity writes. Discover distinct noncanonical source aliases through source indexes and update only exact aliases, leaving canonical image/Removed/collection identities untouched. Retain cache snapshot/restoration, alias-collision recovery and transaction semantics. Time normalization, snapshot, restoration and commit without paths or owner IDs. Validate one covering image index `(id, invoke_source_id, invoke_owner_id, invoke_scope_hidden)` against the unchanged collection query using migrated synthetic catalogs, alternating read benchmarks, write cost and index size/build time. Ship only after repeatable improvement and identical scoped results; append migration 80 if still available. Separate source-open/schema/owner-summary and gallery/statistic query timings without changing scheduling. If the index fails or remaining costs prevent acceptance, return to planning rather than add caches, indexes or progressive UI speculatively.
7. Evaluate exactly two query-only collection-count candidates against frozen current SQL: membership in all visible image IDs, and membership in visible IDs restricted to visible-collection members. Both retain the scoped views and explicit hidden-image exclusion. Use bundled SQLite, migrated synthetic catalogs, and the actual source-discovery queries against a generated Invoke fixture. Ship one global candidate only after exact result equality, at least 20% improvement in both representative concurrent All users/owner medians, and no other median regression above 10% (allow +20 ms when baseline is below 200 ms). No index, migration, persistent cache, new API or startup scheduling change. If neither qualifies, retain the current SQL.

### Query-only experiment gates

- Catalogs: representative 150k images / 132k unique-image memberships / 379 collections; overlapping 150k / 250k; sparse 150k / 1k; small 10k / 1k; empty memberships. Include truly non-Invoke data as well as All users and selected-owner cases. Active correctness fixtures additionally cover legacy, hidden/shared/other-source rows, suppression/board verification, empty/multiple memberships, removal/restoration and scope changes.
- Six counterbalanced variant orders, fresh connections and identical settings; each sample has one first read and five warm reads. The 20% primary improvement gate applies to concurrent warm medians; first-read medians retain the 10%/+20 ms non-regression gate. Retrieve all rows before stopping the timer; compare sorted results afterward. Record SQLite version and query plans. Fresh-connection measurements are not OS-cold startup evidence.
- Run representative concurrent All users/owner comparisons first. A failure of either required primary improvement disqualifies a candidate; if both are disqualified, stop and explicitly mark the remaining performance cells unrun. Missing cells never permit a winner. Otherwise complete the entire non-regression matrix before integration.
- Select the qualifying candidate with the lowest worst-case representative median ratio; ties within 1% favor the simpler visible-ID query. Require indexed membership access and no repeated correlated full-library scan, not a particular COVERING label.
- Preserve mapping, empty counts, smart-count caches, thumbnails, diagnostics, error propagation and fully prepared opening. Run targeted tests, broader checks and independent review. No app launch, import, real-library copy/replacement, production or credential change. A qualifying implementation then needs five regular-dev warm and three four-new-Odin-image launches; readiness and new-image arrival are separate measurements. End-to-end performance remains open until owner acceptance.

## Verification and acceptance

Use red/green tests at hook, command and migrated SQLite seams, then typecheck, lint, frontend/Rust suites, generated bindings and native compatibility build. Independently review persistence/concurrency changes.

Benchmark at least five unchanged-profile launches and three controlled dirty-cache launches; report individual timings, median, slowest, and dominant phase. Separate compilation, migrations and genuinely required full rebuilds from ordinary startup. Targets on the large catalog are under 10 seconds for an unchanged repeat launch and under 30 seconds for a normal cold launch; these are targets, not established capabilities. If the bounded changes cannot meet them, return to planning with measurements rather than widening the refactor.

Native smoke must cover opening, virtualized browsing, Statistics, owner switching and background repair. Automated correctness, review and owner/native acceptance are separate gates.

## Non-goals

No partially prepared opening, weaker privacy protection, persistent word-cloud database, thumbnail-engine rewrite, historical migration edits, production-profile mutation for tests, or raw diagnostics UI.

## Evidence / remaining gates

- Query-only outcome (2026-09-04): both candidates failed the mandatory All users primary gate, so the shipping count SQL is unchanged. Bundled SQLite 3.46.0 completed 216 count samples across six counterbalanced rounds per scope, with matching results and indexed-membership plans. All users warm medians: current 460.363 ms, visible IDs 591.242 ms (+28.4%), membership-restricted IDs 587.690 ms (+27.7%). Selected-owner medians: current 528.658 ms, visible IDs 700.594 ms (+32.5%), membership-restricted IDs 371.772 ms (-29.7%); host-load variability limits interpretation. The selected-owner benefit cannot qualify a global query that regresses All users. All 15 remaining non-regression cells were intentionally unrun after rejection; no small/non-Invoke performance claim follows.
- Query-only evidence is retained in `C:/Users/Artemis/AppData/Local/Temp/ambit-query-only-primary-1788525499169.jsonl`: all 36 six-sample records, six query plans, both cell summaries and environment. First-read medians were All users 524.415 / 636.926 / 622.291 ms and owner 486.723 / 764.987 / 397.790 ms, in baseline/visible/member order. These are direct debug-test SQLite connections and generated source SQL, not frontend IPC, pool validation, controlled OS-cold launches or app acceptance. The recorded executable initially applied the 20% gate to first reads too; corrected warm-only qualification plus first-read non-regression was replayed against the unchanged raw measurements and still rejects both. Failure-only source-thread joining was hardened after the successful run; no measured query/workload changed.
- No query, schema, API or startup behavior changed in this phase. The retained test-only harness protects exact scoped counts and qualification rules. Six new active tests pass, including a witnessed red/green timing-gate test; the full native suite passes 917 library tests (seven ignored) plus 18 utility tests. The frontend suite (3,403 passed, one intentional skip), typecheck, lint, binding drift, Rust formatting, whitespace and guarded frontend build pass. Generated catalog directories were cleaned; only the readable JSONL evidence remains. Hash comparison confirms no changes to pre-existing runtime edits. No app, installer or end-to-end acceptance journey was run; the desktop compatibility build and full coverage/release gate were not rerun for this test-only phase. Latest identity-fix startup timings still require an actual regular-dev journey before choosing another optimization.
- Review closure: the initially over-strict first-read primary gate was corrected to the approved non-regression limit and independently tested; source threads now join on unwind before generated-file cleanup. Separate spec and standards review covered the scoped test changes. A later suggestion to restore the stricter first-read gate was rejected because it contradicted the explicitly approved warm-primary criterion; no runtime behavior was changed for review findings.
- Query-only planning evidence: regular dev has 146,174 images, 131,741 memberships across 379 collections, and every member image has one membership. Python SQLite 3.45 read-only probes returned equal counts but no warm improvement (current about 0.88 seconds; membership-restricted candidate 1.34–1.42 seconds). These exploratory, order-sensitive results are not the bundled-runtime gate and do not justify a shipping rewrite.
- Current boundary: the no-op identity-write regression failed against the previous implementation and passes after exact-alias updates. The full frontend suite (3,403 passed, one intentional skip), native library suite (911 passed, six ignored), and utility tests (18 passed) pass. Typecheck, lint, generated binding drift, Rust formatting, whitespace checks, guarded frontend build and release-profile no-bundle Tauri compatibility build pass. Independent standards/spec reviews and the test-harness closure review are clean. The build guard passed when rerun serially after overlapping builds collided in the shared output directory. No installer, complete coverage/release gate or owner acceptance launch was performed; real startup timing remains unverified.
- Collection-index gate: the tiny migrated fixture selected a covering lookup, but the 150k-row, 2 KiB metadata fixture reported `SEARCH i USING INDEX idx_images_collection_scope_covering_v1 (id=?)`, not a covering lookup. This rejects the proposed migration under the approved gate. No index, migration, query rewrite or cache-lifecycle shortcut is shipped. Preserve the opt-in synthetic experiment for the next query-access-path proposal; its first reads are not OS-cold startup measurements.
- Alternating-order synthetic evidence: matching collection results, warm medians 1,408.354 ms baseline / 676.784 ms candidate; first-read medians 1,400.686 / 704.677 ms. Candidate build took 868–894 ms and added 5,820,416 bytes. Two-thousand-operation insert/update rollback medians were 427.236 / 348.266 ms with noisy samples; this does not establish faster writes. The candidate has a repeatable read benefit but fails the explicitly required covering-plan gate, so its opt-in experiment intentionally remains red and no migration is registered. These are standalone generated-catalog measurements, not app acceptance or controlled OS-cold results; unrelated host activity was present. Generated catalogs self-cleaned; no real library was copied or indexed. The exact optimizer reason for the non-covering plan remains unproven.
- Benchmark precision: the recorded timings above used the equivalent count query ordered by collection ID, while the failed query-plan gate inspected the unchanged production SQL. The retained harness now times unchanged SQL and sorts returned rows outside the timer for comparison; this final harness correction was compiled, not re-benchmarked. Scope-count correctness remains an active regression; the rejected index experiment alone is opt-in. No performance claim should extrapolate these measurements to the real app.
- Latest owner test (2026-09-04): stopwatch 54.14 seconds initial, 15.23 seconds unchanged, 74.17 seconds after four Odin images under All users. Last-run diagnostics show initial collection counting 29.115 seconds overlapping discovery 31.385 seconds, native owner visibility 27.043 seconds with zero transaction-entry wait, restored-cache refresh 6.642 seconds, and first-page loading 10.145 seconds. Splash ended at process 73.164 seconds but readiness was 82.992 seconds; compilation was 2.01 seconds. This fails changed-source acceptance and does not establish an unavoidable multi-account cost.
- Approved follow-up keeps the fully prepared opening and evaluated one additive index, now rejected at its gate. No real library copy, automatic launch/import, production changes or credential changes. Collection/query costs also affect non-Invoke libraries; configured single-owner Invoke sources still run discovery/preparation. Next step is a revised collection access-path proposal before requesting another acceptance cycle; any future index-creation cost must be reported separately from warm repeats.
- Lock-remediation verification: frontend 3,389 passed with one intentional skip; native library 909 passed with five intentional ignores and utility binary 18 passed. TypeScript, lint, binding drift, guarded frontend build, Rust formatting, whitespace checks and release-profile no-bundle Tauri build passed. Native tests used the equivalent `cargo test --lib --bins` targets with normal Tauri build prerequisites enabled after the wrapper's skipped capability generation failed following a disk-full build. Two regenerable incremental caches from this run were removed after exact-path/reparse validation; no library or backup was removed. Independent standards and spec reviews closed clean; a pre-existing timestamp-cursor completeness limitation is recorded in `docs/refactor.md`. No installer, complete coverage/release gate, or owner acceptance launch was performed.
- Failed native acceptance (2026-09-04): after four new Odin images, owner discovery completed but preparation displayed `database is locked`. Collection counting still took 22.602 seconds. The error handler discarded operation attribution, and `startInvokeSync` discarded its blocked outcome. The original warm-start evidence therefore does not establish changed-source acceptance.
- Contention remediation: fully migrated synthetic file databases with separate connections reproduced `database is locked` in both actual visibility and board-reconciliation routines while a collection-cache writer held a brief transaction. Acquiring `BEGIN IMMEDIATE` before reads made both cases pass. Short-budget exhaustion, non-lock failure, rollback, and timeout restoration are covered. This establishes an Ambit-side defect, not proof that it was the only cause of the reported live failure. No app or library copy was used.
- Diagnostics now distinguish fingerprint reads, source repair, board reads/writes, visibility and collection rows/counts, with fixed database roles, attempt and failure classification. Native transaction-entry logs retain the integer SQLite extended code and wait duration without raw error data. The diagnostic IPC extension is additive; bindings are generated. Sync outcome propagation is an internal TypeScript contract correction; the manual-sync UI callback still returns void.
- Read-only query probe using bundled SQLite on regular dev: current query 44.057 / 1.975 / 1.760 seconds; membership-first equivalent 1.978 / 1.916 / 2.329 seconds. Both use membership and image-ID indexes. This was a standalone connection with default connection settings, during other verification work; the first read is not an OS-cold benchmark, and ordering warmed the alternative. It does not establish a faster query or acceptable startup. No runtime query rewrite, index, cache, or migration was added in this remediation.
- Next acceptance uses regular dev with InvokeAI open: five unchanged launches and three four-image changed-source launches. Log gallery readiness, transaction waits, collection phases, and arrival of the four images separately. If collections still dominate, return to a measured access-path/cache proposal rather than silently expanding this package. Production-release and owner/recovery journeys remain pending.
- Changed-source verification (2026-09-04): frontend 3,377 passed with one intentional skip; Rust library 903 passed with four ignored and utility binary 18 passed. Strict TypeScript, lint, binding drift, guarded frontend build and whitespace checks pass. Separate standards and spec reviews closed clean after source-shrinkage/timestamp-regression guards. The migrated SQLite test executes the actual frontend count SQL and verifies visible/shared/hidden collections plus indexed membership and image-ID lookups. No native launch, library mutation, installer build or full release/coverage gate was performed in this follow-up; experiential acceptance remains pending.
- Changed-source review: source-count decreases and backward/disappearing maximum image timestamps retain full repair (red/green regressions). Aggregate fingerprints are not an append-only proof: the All users view intentionally uses cached owner facts until timestamp catch-up or authoritative owner/forced reconciliation. This does not authorize using the aggregate shortcut for selected-owner admission. Full inventory reconciliation clears missing owner assignments; it does not delete retained Ambit images.
- Approved follow-up: isolate debug migration-history repair to its active database, preserving release compatibility. A regression reproduced an inactive production-fixture checksum update before the fix; the fixed debug path leaves its bytes and migration records unchanged. Debug and release migration suites, full frontend/Rust suites, lint and strict TypeScript pass. The additive debug test identifier is `com.ambit.startup-test`; normal dev and installed production remain separate.
- The additive debug test copy contained 295,113 Ambit image records, a 156,021-image InvokeAI snapshot, and 290,584 copied Ambit thumbnail files. Its database and path-integrity checks passed, but owner evaluation found that missing source media and the separate launch/profile flow made the experience unrepresentative. The approach was withdrawn and its disposable profile was removed. The independently hash-verified dev database/settings backup is retained outside the test folder.
- Regular dev was expanded to roughly 150k images (146,166 observed), with InvokeAI ownership enabled. The owner reports five unchanged warm starts of 7–10 seconds. This is representative large-library warm evidence, not an OS-cold or production-release measurement.
- After creating four Odin images, All users startup took 165,684 ms. Logs attribute 73,410 ms to full location mapping, 33,244 ms to full source details, and 24,911 ms to initial collections (24.805 seconds in its count query). Owner discovery overlaps collections and must not be added to that duration. Maintenance began after readiness; later four-row insertion took 50 ms. Ordinary fingerprint drift incorrectly selected the full source-repair path.
- Changed-source acceptance must use regular dev and the actual InvokeAI connection: retain the 7–10-second unchanged baseline, then create four Odin images and measure gallery readiness separately from background catch-up. Target gallery readiness is at most 15 seconds with no full source mapping/details before readiness; verify the four images arrive and selected-owner/privacy behavior remains correct. This target is not yet measured. Initial sync, owner switches and OS-cold starts remain separate acceptance cases.
- Worktree updated from release `edc439a8` to `20cd2390` on `fix/startup-readiness`.
- Latest-main frontend baseline: 278 files, 3,352 passing tests, one intentional skip.
- Implemented readiness-gated maintenance, Statistics-only analysis, selective thumbnail invalidation and bounded local startup diagnostics. Owner/privacy and splash presentation behavior is unchanged.
- Standards and spec reviews closed clean after fixing metadata duplicate/stale-retry races and preserving foreground throttling during ordinary filtering.
- Final verification: frontend suite 3,364 passed with one intentional skip across 279 files; Rust library suite 900 passed with four ignored, plus 18 support-inspection binary tests passed. Lint, strict TypeScript, generated binding drift, guarded frontend build, release-profile no-bundle Tauri compatibility build and diff whitespace checks passed. An optimizer root-probe timing test failed once during a focused run, then passed both its exact rerun and the full suite. The complete packaging/release gate and coverage were not run.
- Real-catalog startup timing and native user acceptance remain unverified. Initial read-only feasibility checks found that a custom release identifier is not sufficient isolation: release database migration/path repair still targets the production identifier, and keyring identity is shared. The later approved debug-only repair guard and repointed test copy do not solve release-profile isolation. Production and normal-dev profiles were not replaced; the installed production app was not launched by this work.
- Do not recreate the withdrawn isolated test setup. Continue acceptance in regular dev without replacing either library, changing credentials or launching/importing automatically. A true OS-cold timing must be distinguished from a warm repeat. Production-release performance remains open.
